import { Router } from "express"; import { integrationTestService } from "../services/genericService"; const router = Router(); router.all("/gateway/*", async (req, res) => { try { const result = await integrationTestService.executeOperation("api_gateway", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;