import { Router } from "express"; import { securityTestService } from "../services/genericService"; const router = Router(); router.get("/timing-analysis", async (req, res) => { try { const result = await securityTestService.executeOperation("timing_analysis", req.query); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;