import { Router } from "express"; import { validationService } from "../services/genericService.js"; const router = Router(); router.post("/validate", async (req, res) => { try { const result = await validationService.executeOperation("data_validation", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;