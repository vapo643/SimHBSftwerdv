import { Router } from "express"; import { documentService } from "../services/genericService.js"; const router = Router(); router.get("/preview/:id", async (req, res) => { try { const result = await documentService.executeOperation("file_preview", { id: req.params.id }); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;