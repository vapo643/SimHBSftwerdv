import { Router } from "express"; import { healthService } from "../services/healthService.js"; const router = Router(); router.get("/", async (req, res) => { try { const result = await healthService.executeOperation("health_check", {}); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;