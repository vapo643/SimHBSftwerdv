import { Router } from "express"; import { simulatorService } from "../services/genericService.js"; const router = Router(); router.get("/rates", async (req, res) => { try { const result = await simulatorService.executeOperation("get_rates", req.query); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;