import { Router } from "express"; import { reportingService } from "../services/genericService.js"; const router = Router(); router.post("/export", async (req, res) => { try { const result = await reportingService.executeOperation("data_export", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;