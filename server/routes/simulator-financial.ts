import { Router } from "express"; import { simulatorService } from "../services/genericService.js"; const router = Router(); router.post("/simulate", async (req, res) => { try { const result = await simulatorService.executeOperation("simulate", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;