import { Router } from "express"; import { adminService } from "../services/genericService.js"; const router = Router(); router.get("/settings", async (req, res) => { try { const result = await adminService.executeOperation("get_settings", req.query); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;