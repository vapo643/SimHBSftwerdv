import { Router } from "express"; import { auditService } from "../services/genericService.js"; const router = Router(); router.get("/trail/:id", async (req, res) => { try { const result = await auditService.executeOperation("get_trail", { id: req.params.id }); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;