import { Router } from "express"; import { integrationTestService } from "../services/genericService.js"; const router = Router(); router.post("/manage", async (req, res) => { try { const result = await integrationTestService.executeOperation("webhook_manager", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;