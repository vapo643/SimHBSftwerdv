import { Router } from "express"; import { testRetryService } from "../services/testService.js"; const router = Router(); router.post("/retry", async (req, res) => { try { const result = await testRetryService.executeOperation("retry", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;