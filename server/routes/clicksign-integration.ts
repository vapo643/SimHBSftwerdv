import { Router } from "express"; import { clicksignService } from "../services/genericService.js"; const router = Router(); router.post("/integrate", async (req, res) => { try { const result = await clicksignService.executeOperation("integrate", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;