import { Router } from "express"; import { monitoringService } from "../services/monitoringService.js"; const router = Router(); router.get("/performance", async (req, res) => { try { const result = await monitoringService.executeOperation("performance_check", req.query); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;