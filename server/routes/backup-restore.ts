import { Router } from "express"; import { backupService } from "../services/genericService.js"; const router = Router(); router.post("/restore", async (req, res) => { try { const result = await backupService.executeOperation("restore_backup", req.body); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;