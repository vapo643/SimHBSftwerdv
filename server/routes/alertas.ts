import { Router } from "express"; import { alertService } from "../services/genericService"; const router = Router(); router.get("/", async (req, res) => { try { const result = await alertService.executeOperation("list_alerts", req.query); res.json(result); } catch (error: any) { res.status(500).json({ success: false, error: error.message }); } }); export default router;