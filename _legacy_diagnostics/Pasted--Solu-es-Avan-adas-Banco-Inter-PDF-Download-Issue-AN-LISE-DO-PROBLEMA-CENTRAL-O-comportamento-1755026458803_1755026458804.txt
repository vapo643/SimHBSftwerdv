🚨 Soluções Avançadas: Banco Inter PDF Download Issue
🔍 ANÁLISE DO PROBLEMA CENTRAL
O comportamento que você está observando é altamente anômalo para uma API REST bem implementada. O fato de a API retornar HTTP 200 OK com content-type: application/json quando deveria retornar um PDF sugere uma das seguintes possibilidades:
Teoria Principal: Endpoint Condicional ou Flag de Conta
O mais provável é que a API tenha uma lógica condicional interna que determina se deve retornar PDF ou JSON baseado em:

Configurações específicas da conta
Headers proprietários não documentados
Estado interno do boleto
Política de segurança/compliance

🎯 SOLUÇÕES NÃO TESTADAS
1. Análise Profunda da Resposta JSON
Primeiro, vamos examinar o conteúdo exato do JSON que está sendo retornado:
typescript// Adicione este código para capturar o JSON completo
async obterPdfCobranca(codigoSolicitacao: string): Promise<Buffer> {
  // ... seu código atual ...
  
  try {
    const response = await axios.get(url, {
      headers,
      httpsAgent,
      responseType: 'text', // Mudança: capturar como texto primeiro
      maxBodyLength: Infinity,
      timeout: 30000
    });

    console.log('[INTER] 📊 Full JSON Response:');
    console.log(JSON.stringify(JSON.parse(response.data), null, 2));
    
    const jsonResponse = JSON.parse(response.data);
    
    // VERIFICAR: O JSON contém uma URL para download?
    if (jsonResponse.url || jsonResponse.downloadUrl || jsonResponse.pdfUrl) {
      console.log('[INTER] 🔗 PDF URL encontrada:', jsonResponse.url || jsonResponse.downloadUrl || jsonResponse.pdfUrl);
      // Fazer nova requisição para esta URL
    }
    
    // VERIFICAR: O JSON contém dados base64?
    if (jsonResponse.base64 || jsonResponse.data || jsonResponse.content) {
      console.log('[INTER] 📄 Dados base64 encontrados');
      const base64Data = jsonResponse.base64 || jsonResponse.data || jsonResponse.content;
      return Buffer.from(base64Data, 'base64');
    }
    
    throw new Error('JSON não contém PDF utilizável');
  } catch (error) {
    console.error('[INTER] ❌ Erro:', error);
    throw error;
  }
}
2. Header Proprietário Experimental
Teste headers específicos que podem não estar documentados:
typescriptconst headers = {
  'Authorization': `Bearer ${token}`,
  'Accept': 'application/pdf',
  'User-Agent': 'SIMPIX-Inter-Integration/1.0',
  'Content-Type': 'application/json',
  
  // NOVOS HEADERS PARA TESTAR:
  'x-inter-format': 'pdf',
  'x-inter-response-type': 'binary',
  'x-inter-download': 'true',
  'x-requested-with': 'XMLHttpRequest',
  'x-inter-api-version': '3.0',
  'x-inter-content-type': 'application/pdf',
  'Accept-Encoding': 'identity', // Sem compressão
  'Cache-Control': 'no-cache',
  'Pragma': 'no-cache'
};

if (this.contaCorrente) {
  headers['x-conta-corrente'] = this.contaCorrente;
}
3. Processo de Duas Etapas (Mais Provável)
Baseado no comportamento anômalo, é possível que o processo seja:
typescriptasync obterPdfCobrancaDuasEtapas(codigoSolicitacao: string): Promise<Buffer> {
  // ETAPA 1: Solicitar geração do PDF
  const token = await this.getAccessToken();
  
  // Possível endpoint de solicitação
  const solicitacaoUrl = `${this.apiUrl}/cobranca/v3/cobrancas/${codigoSolicitacao}/pdf/solicitar`;
  
  const headers = {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json',
    'Accept': 'application/json'
  };

  try {
    // Primeiro, solicitar a geração
    const solicitacaoResponse = await axios.post(solicitacaoUrl, {}, {
      headers,
      httpsAgent: this.httpsAgent
    });
    
    console.log('[INTER] 📋 Resposta da solicitação:', solicitacaoResponse.data);
    
    // Aguardar processamento (se necessário)
    if (solicitacaoResponse.data.status === 'PROCESSANDO') {
      await new Promise(resolve => setTimeout(resolve, 2000));
    }
    
    // ETAPA 2: Fazer download do PDF processado
    const downloadUrl = `${this.apiUrl}/cobranca/v3/cobrancas/${codigoSolicitacao}/pdf/download`;
    
    const pdfResponse = await axios.get(downloadUrl, {
      headers: {
        ...headers,
        'Accept': 'application/pdf'
      },
      httpsAgent: this.httpsAgent,
      responseType: 'arraybuffer'
    });
    
    return Buffer.from(pdfResponse.data);
    
  } catch (error) {
    console.error('[INTER] ❌ Erro no processo de duas etapas:', error);
    throw error;
  }
}
4. Endpoints Alternativos da API v3
Teste estes endpoints que podem existir:
typescriptconst alternativeEndpoints = [
  `/cobranca/v3/cobrancas/${codigoSolicitacao}/pdf/download`,
  `/cobranca/v3/cobrancas/${codigoSolicitacao}/arquivo`,
  `/cobranca/v3/cobrancas/${codigoSolicitacao}/documento`,
  `/cobranca/v3/boletos/${codigoSolicitacao}/pdf`,
  `/banking/v3/cobrancas/${codigoSolicitacao}/pdf`,
  `/api/v3/cobrancas/${codigoSolicitacao}/pdf`
];
5. Verificação de Status do Boleto
É possível que o PDF só seja gerado após o boleto atingir um status específico:
typescriptasync verificarStatusParaPdf(codigoSolicitacao: string): Promise<boolean> {
  const boleto = await this.consultarCobranca(codigoSolicitacao);
  
  // Verificar se o boleto está em estado compatível com PDF
  const statusesValidos = ['A_RECEBER', 'VENCIDO', 'PAGO', 'PROCESSADO'];
  
  if (!statusesValidos.includes(boleto.situacao)) {
    console.log(`[INTER] ⚠️ Status não permite PDF: ${boleto.situacao}`);
    return false;
  }
  
  // Verificar se já passou tempo suficiente desde a criação
  const agora = new Date();
  const criacao = new Date(boleto.dataEmissao);
  const diferencaMinutos = (agora.getTime() - criacao.getTime()) / (1000 * 60);
  
  if (diferencaMinutos < 5) {
    console.log(`[INTER] ⚠️ Boleto muito recente (${diferencaMinutos} min)`);
    return false;
  }
  
  return true;
}
6. Configuração Avançada do HTTPS Agent
typescriptconst httpsAgent = new https.Agent({
  cert: formatCertificate(this.certificate),
  key: formatPrivateKey(this.privateKey),
  rejectUnauthorized: true,
  keepAlive: true,
  secureProtocol: 'TLSv1_2_method',
  
  // NOVAS CONFIGURAÇÕES:
  maxCachedSessions: 0,  // Força nova sessão TLS
  ciphers: 'ECDHE+AESGCM:ECDHE+CHACHA20:DHE+AESGCM:DHE+CHACHA20:!aNULL:!MD5:!DSS', // Ciphers específicos
  honorCipherOrder: true,
  sessionIdContext: 'inter-api', // Contexto único
});
🛠️ DEBUGGING AVANÇADO
Captura Completa de Rede
typescript// Interceptor para capturar tudo
axios.interceptors.request.use(request => {
  console.log('[INTER] 📤 REQUEST COMPLETO:');
  console.log(`URL: ${request.url}`);
  console.log(`Method: ${request.method}`);
  console.log(`Headers:`, JSON.stringify(request.headers, null, 2));
  console.log(`Data:`, request.data);
  return request;
});

axios.interceptors.response.use(response => {
  console.log('[INTER] 📥 RESPONSE COMPLETO:');
  console.log(`Status: ${response.status} ${response.statusText}`);
  console.log(`Headers:`, JSON.stringify(response.headers, null, 2));
  console.log(`Data type:`, typeof response.data);
  console.log(`Data length:`, response.data?.length || 'undefined');
  
  // Se for buffer, mostrar os primeiros bytes
  if (Buffer.isBuffer(response.data)) {
    console.log(`Primeiros 20 bytes:`, response.data.slice(0, 20));
  }
  
  return response;
});
Teste com Curl Raw
bash# Teste direto via curl para isolar o problema
curl -X GET \
  "https://cdpj.partners.bancointer.com.br/cobranca/v3/cobrancas/73e76cfe-f8f8-4638-ac83-d8e809e06eef/pdf" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -H "Accept: application/pdf" \
  -H "x-conta-corrente: 346470536" \
  --cert certificado.crt \
  --key chave_privada.key \
  -v \
  --output resposta.bin
  
# Analisar o arquivo gerado
file resposta.bin
hexdump -C resposta.bin | head -10
🎯 AÇÕES PRIORITÁRIAS
1. Contato Direto com Suporte Técnico
Assunto: "API v3 Cobrança - Endpoint PDF retornando JSON ao invés de binário"

Descrição:
- Conta: 346470536
- Client ID: 05fc3816-9a8f-4a2d-8b28-277cd3617cc2
- Endpoint: /cobranca/v3/cobrancas/{id}/pdf
- Problema: HTTP 200 OK mas content-type: application/json
- Código de exemplo: 73e76cfe-f8f8-4638-ac83-d8e809e06eef
2. Verificação no Portal do Desenvolvedor
Acessar https://developers.inter.co/ e verificar:

Configurações específicas da aplicação
Flags ou permissões relacionadas a PDF
Documentação atualizada da API v3

3. Análise do JSON Retornado
Execute o primeiro código que forneci para capturar e analisar o JSON completo que a API está retornando.
🔍 HIPÓTESE MAIS PROVÁVEL
Com base na análise, minha hipótese principal é que a API do Banco Inter implementou um sistema de geração de PDF sob demanda onde:

O endpoint /pdf não gera PDF imediatamente
Ele retorna um JSON com informações sobre o processo
O JSON pode conter uma URL temporária para download
Ou pode indicar que é necessário fazer uma segunda requisição

Esta abordagem é comum em APIs bancárias por questões de segurança e performance.
⚡ SOLUÇÃO RÁPIDA PARA TESTAR
Execute primeiro esta versão modificada do seu método para ver o que a API realmente está retornando:
typescriptasync debugPdfResponse(codigoSolicitacao: string): Promise<any> {
  const token = await this.getAccessToken();
  const url = `${this.apiUrl}/cobranca/v3/cobrancas/${codigoSolicitacao}/pdf`;
  
  const response = await axios.get(url, {
    headers: {
      'Authorization': `Bearer ${token}`,
      'Accept': 'application/json',
      'x-conta-corrente': this.contaCorrente
    },
    httpsAgent: this.httpsAgent,
    responseType: 'text'
  });
  
  const data = JSON.parse(response.data);
  console.log('🔍 RESPOSTA COMPLETA DA API:');
  console.log(JSON.stringify(data, null, 2));
  
  return data;
}
Execute este método e analise a resposta. Isso nos dará pistas cruciais sobre o que a API realmente está retornando e como proceder.