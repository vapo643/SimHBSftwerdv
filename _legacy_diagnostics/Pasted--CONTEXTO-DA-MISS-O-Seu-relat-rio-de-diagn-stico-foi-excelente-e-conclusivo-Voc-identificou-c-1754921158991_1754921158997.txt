**CONTEXTO DA MISSÃO:**
Seu relatório de diagnóstico foi excelente e conclusivo. Você identificou corretamente que a causa raiz dos campos ausentes na CCB é a falta de inputs no formulário de "Nova Proposta", junto com um bug na renderização do endereço e na persistência dos dados bancários.

Sua missão agora é executar o roadmap de correção cirúrgico abaixo, baseado nas suas próprias descobertas.

**PROTOCOLO OBRIGATÓRIO (Roadmap de Correção):**

**1. Prioridade 1: Adicionar os Campos Ausentes no Frontend (A Causa Raiz):**
    - **Ação:** Modifique o ficheiro `client/src/pages/nova-proposta.tsx`.
    - **Implementação:** Na seção de documentos do cliente, adicione os seguintes campos de input que estão faltando:
        - `Local de Nascimento` (campo de texto).
        - `Data de Emissão do RG` (campo de data).
        - `UF do RG` (campo de texto, 2 caracteres).
    - **Validação:** Garanta que estes novos campos sejam corretamente adicionados ao estado do formulário React e enviados ao backend na submissão da proposta.

**2. Prioridade 2: Corrigir a Persistência dos Dados Bancários:**
    - **Ação:** Seu diagnóstico mostrou que os campos de dados bancários existem no frontend, mas os dados não chegam ao PDF. Investigue a rota `POST /api/propostas`.
    - **Implementação:** Revise a lógica de salvamento com Drizzle e garanta que os dados (`dadosPagamentoBanco`, `dadosPagamentoAgencia`, etc.) sejam corretamente salvos no banco de dados. O bug está nesta camada de persistência.

**3. Prioridade 3: Corrigir a Renderização do Endereço no PDF:**
    - **Ação:** Modifique o ficheiro `server/services/ccbGenerationService.ts`.
    - **Implementação:**
        a. Remova a lógica que concatena o endereço em uma única string (`enderecoMontado`).
        b. Implemente chamadas `page.drawText()` **separadas** para cada parte do endereço (`logradouro`, `numero`, `bairro`, `cidade`, `estado`, `cep`), utilizando as coordenadas exatas já presentes no ficheiro `ccbUserCoordinates.ts`.

**4. Validação Final (Protocolo 5-CHECK):**
    - **Ação Obrigatória:** Após implementar TODAS as correções acima, execute `get_latest_lsp_diagnostics` e corrija qualquer erro de LSP.
    - **Teste Obrigatório:** Crie uma **NOVA PROPOSTA** na UI, preenchendo **TODOS** os campos (incluindo os novos campos de RG). Em seguida, gere a CCB para esta nova proposta.
    - **Relatório Final Obrigatório:** O critério de sucesso é a apresentação de um PDF onde:
        a. O endereço está perfeitamente formatado nos seus campos distintos.
        b. Os dados bancários (Banco, Agência, Conta, PIX) estão preenchidos.
        c. Os novos campos (Local de Nascimento, Data de Emissão do RG, UF do RG) estão preenchidos.