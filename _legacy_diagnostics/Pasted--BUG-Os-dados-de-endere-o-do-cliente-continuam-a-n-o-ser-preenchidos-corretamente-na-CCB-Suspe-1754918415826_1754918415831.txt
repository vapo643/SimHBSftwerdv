**BUG:**
Os dados de endereço do cliente continuam a não ser preenchidos corretamente na CCB. Suspeitamos de uma quebra no fluxo de dados entre o salvamento da proposta e a geração do PDF, ou de um desalinhamento nas chaves de dados e coordenadas utilizadas. Sua missão é uma **auditoria de diagnóstico, não de correção**. Você deve investigar e reportar os fatos.

**PROCESSO SISTEMÁTICO (Roadmap de Auditoria de Dados):**

**1. Auditoria da Persistência de Dados (O Banco de Dados):**
    - **Ação:** Investigue a rota da API `POST` que salva uma "Nova Proposta".
    - **Verificação:** Adicione logs para exibir o payload de endereço que chega ao backend. Em seguida, verifique na tabela `propostas` se este mesmo objeto de endereço estruturado (com rua, cidade, CEP, etc.) está a ser salvo corretamente na coluna `cliente_data`.
    - **Relatório:** Responda objetivamente: **SIM ou NÃO**, os dados completos e estruturados do endereço estão a ser salvos no banco de dados quando uma nova proposta é criada?

**2. Auditoria das "Tags" de Dados (Origem vs. Destino):**
    - **Ação:** Analise o código do formulário de "Nova Proposta" no frontend.
    - **Relatório 1 (Origem):** Qual é o nome exato da chave/variável no estado do React que agrupa os campos do endereço antes de serem enviados para o backend?
    - **Ação:** Agora, analise o serviço `pdfGeneratorService.ts`.
    - **Relatório 2 (Destino):** Qual é o nome exato da chave que a função `generateCcbFromTemplate` espera e da qual ela tenta ler os dados de endereço (ex: `propostaData.cliente_data`, `propostaData.endereco`, etc.)?

**3. Auditoria das Coordenadas (Mapeamento):**
    - **Ação:** O Executor Humano irá anexar um ficheiro `MAPEAMENTO_COMPLETO_CCB_COORDENADAS.md` a este prompt. Este ficheiro é a nossa fonte da verdade.
    - **Verificação:** Dentro do `pdfGeneratorService.ts`, compare as coordenadas (valores X e Y) usadas nas chamadas `page.drawText()` para os campos de endereço com as coordenadas listadas no ficheiro de mapeamento fornecido.
    - **Relatório:** Responda objetivamente: **SIM ou NÃO**, as coordenadas no código-fonte para `Endereço`, `CEP`, e `Cidade` são idênticas às especificadas no documento? Se a resposta for NÃO, liste as coordenadas que estão a ser usadas no código atualmente (as coordenadas "default" ou erradas).

**Relatório Final:**
Sua resposta final deve ser um relatório claro respondendo aos 3 pontos de auditoria acima. Não implemente nenhuma correção ainda.