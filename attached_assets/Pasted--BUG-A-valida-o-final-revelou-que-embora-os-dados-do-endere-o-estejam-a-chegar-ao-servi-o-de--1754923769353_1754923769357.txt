**BUG:**
A validação final revelou que, embora os dados do endereço estejam a chegar ao serviço de geração de PDF, eles não estão a ser posicionados nas coordenadas corretas na CCB. Campos como `Rua + Número`, `CEP`, `Cidade` e `UF` estão a aparecer em locais errados e desordenados.

**PROCESSO SISTEMÁTICO (Roadmap de Auditoria e Correção de Coordenadas):**

**1. Isolamento do Código:**
    - **Ação:** Concentre-se exclusivamente no ficheiro `server/services/pdfGeneratorService.ts` e no ficheiro onde as constantes de coordenadas estão definidas (provavelmente `ccbUserCoordinates.ts`).

**2. Auditoria Cruzada (Código vs. Fonte da Verdade):**
    - **Ação:** O Executor Humano irá anexar o ficheiro `MAPEAMENTO_COMPLETO_CCB_COORDENADAS.md`. Este documento é a **única fonte da verdade** para as coordenadas.
    - **Verificação:** Abra o seu ficheiro de constantes de coordenadas. Lado a lado com o documento de mapeamento, compare **linha por linha** as coordenadas X e Y para **TODOS** os campos de endereço do cliente (emitente).
    - **Relatório de Desvios:** Em sua resposta, liste qualquer discrepância que encontrar entre os valores de X e Y no seu código e os valores na fonte da verdade.

**3. Implementação da Correção:**
    - **Ação:** Corrija **TODAS** as coordenadas no seu ficheiro de constantes para que correspondam **EXATAMENTE** às coordenadas especificadas no `MAPEAMENTO_COMPLETO_CCB_COORDENADAS.md`.
    - **Ação Secundária:** Inspecione a função `generateCcbFromTemplate` e confirme que as chamadas `page.drawText()` para cada parte do endereço estão a usar as constantes de coordenadas corretas que você acabou de corrigir.

**4. Validação Final (Protocolo 5-CHECK):**
    - **Ação Obrigatória:** Execute `get_latest_lsp_diagnostics` para garantir ZERO erros.
    - **Teste Obrigatório:** Gere uma nova CCB de teste.
    - **Relatório Final:** O critério de sucesso é um PDF onde todos os campos de endereço estão visualmente perfeitos, alinhados em seus respectivos quadrados, conforme o design do template e o mapa de coordenadas.

**DADOS REAIS:**
Utilize uma proposta de teste existente que contenha todos os dados de endereço preenchidos.