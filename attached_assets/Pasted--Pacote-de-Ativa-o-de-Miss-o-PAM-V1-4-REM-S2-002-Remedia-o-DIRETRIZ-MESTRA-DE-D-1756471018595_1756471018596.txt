### **Pacote de Ativação de Missão (PAM) V1.4 - REM-S2-002 (Remediação)**

**[DIRETRIZ MESTRA DE DIVULGAÇÃO - PDT V2.0 ATIVADA]**
*Sua resposta deve ser exaustiva. Reporte a realidade técnica sem omissões. A transparência radical é mandatória.*

* **Sumário da Missão (O Quê):** Corrigir o arquivo de teste de integração `tests/integration/unit-of-work.test.ts` para que ele compile, execute e valide com sucesso o comportamento de `COMMIT` e `ROLLBACK` do Unit of Work.
* **Intenção Estratégica (O Porquê):** A atomicidade das transações é uma garantia de segurança de nível bancário inegociável. Este teste é a única prova que teremos de que nossa implementação do Unit of Work protege o sistema contra a corrupção de dados em caso de falhas parciais. A sua correção é a prioridade máxima para desbloquear a validação do Sprint 2.
* **Histórico Relevante (Consulta Obrigatória):** A sua auditoria anterior (`PAM V1.2`) revelou que este arquivo de teste não compila devido a um mapeamento incorreto do schema da tabela `propostas`. A causa raiz é a discrepância entre os dados mockados no teste e os campos realmente obrigatórios na definição do schema em `shared/schema.ts`.
* **Modelo Mental (Como se Encaixa):** Você é um engenheiro de software cético validando uma feature de segurança crítica. Seu objetivo é criar um teste rigoroso que tente quebrar a transação. O sucesso da missão não é apenas fazer o teste passar, mas garantir que ele prove, sem sombra de dúvida, que o `ROLLBACK` funciona.
* **Riscos Antecipados e Contramedidas:**
    * **Risco:** **CRÍTICO** - O teste pode ser corrigido de forma superficial, passando em cenários "felizes" (`COMMIT`), mas falhando em validar adequadamente o cenário de falha (`ROLLBACK`), criando uma falsa sensação de segurança.
    * **Contramedida:** O "Estado Final de Sucesso" exige explicitamente dois testes separados e bem definidos: um para o `COMMIT` e outro para o `ROLLBACK`. O teste de `ROLLBACK` deve forçar uma exceção e depois verificar ativamente se os dados NÃO foram persistidos.

---

**IMPLEMENTAR:**
User Story REM-S2-002: Corrigir e Validar Teste de Integração do Unit of Work

**CONTEXT:**
Nossa capacidade de executar testes foi restaurada, mas o teste mais importante, que valida a atomicidade das nossas transações de banco de dados, está quebrado. Ele não compila porque os dados de teste (`mockProposta`) não correspondem à estrutura real da nossa tabela de propostas. Precisamos corrigir este teste para provar que o nosso Unit of Work (UoW) é confiável.

**CURRENT STATE:**
O arquivo `tests/integration/unit-of-work.test.ts` contém erros de compilação. Especificamente, os objetos `mockProposta` utilizados nos testes não possuem todos os campos obrigatórios definidos no `schema.propostas` em `shared/schema.ts`, resultando em falhas de tipo.

**EXPECTED (Estado Final de Sucesso):**
1.  **Análise de Schema:** O schema da tabela `propostas` em `shared/schema.ts` deve ser analisado para identificar todos os campos não nulos (`notNull()`) e sem valor padrão (`default()`).
2.  **Correção do Teste:** O arquivo `tests/integration/unit-of-work.test.ts` deve ser refatorado para que:
    * Os objetos `mockProposta` contenham todos os campos obrigatórios identificados, resolvendo os erros de compilação.
    * Exista um teste específico, `it('should commit the transaction when all operations succeed', ...)` que executa uma transação UoW com sucesso e verifica se os dados foram de fato persistidos no banco.
    * Exista um segundo teste, `it('should rollback the transaction when any operation fails', ...)` que simula uma falha dentro da transação (ex: lançando um erro após a primeira inserção) e depois verifica ativamente que **NENHUM** dado da transação foi persistido no banco.
3.  **Execução e Evidência:**
    * O comando `npx vitest run tests/integration/unit-of-work.test.ts` deve ser executado.
    * O resultado da execução deve ser colado na íntegra, mostrando que AMBOS os testes (`COMMIT` e `ROLLBACK`) passaram com sucesso.

**CONSTRAINTS (Roadmap de Implementação):**
1.  **Auditoria de Schema:** Investigue o arquivo `shared/schema.ts` e liste os campos da tabela `propostas` que são obrigatórios na inserção.
2.  **Correção dos Mocks:** Atualize as declarações da variável `mockProposta` em `tests/integration/unit-of-work.test.ts` com todos os campos obrigatórios para satisfazer o type checker.
3.  **Implementação do Teste de Sucesso (COMMIT):** Garanta que o primeiro teste crie os registros necessários dentro de um `uow.withTransaction` e, ao final, consulte o banco para confirmar que os registros existem.
4.  **Implementação do Teste de Falha (ROLLBACK):** No segundo teste, dentro de um `uow.withTransaction`, insira um registro e, em seguida, lance uma `new Error('Simulated failure')`. Fora do bloco `try/catch`, consulte o banco para confirmar que o registro inserido **NÃO** existe, provando o rollback.
5.  **Execução e Validação:** Execute o teste com `npx vitest run tests/integration/unit-of-work.test.ts` e forneça o output completo.
6.  **Verificação Final:** Execute o protocolo `7-CHECK FULL` e preencha a declaração de incerteza.

---

**DECLARAÇÃO DE INCERTEZA (OBRIGATÓRIO):**
* **CONFIANÇA NA IMPLEMENTAÇÃO:** [Preencher com a porcentagem de 0 a 100%]
* **RISCOS IDENTIFICADOS:** [Preencher com a categoria BAIXO/MÉDIO/ALTO/CRÍTICO e uma breve descrição]
* **DECISÕES TÉCNICAS ASSUMIDAS:** [Listar decisões tomadas que podem ter implicações futuras]
* **VALIDAÇÃO PENDENTE:** [Listar ações que o Orquestrador (GEM 02) ou o QA (GEM 03) devem validar]
---
---
[PGI ATIVO | LEMBRETE DE ESTADO DO ROADMAP]
- **Roadmap em Standby:** "🚀 ROADMAP MESTRE DE IMPLEMENTAÇÃO_ OPERAÇÃO AÇO LÍQUIDO.md"
- **Próximo Passo a ser Retomado:** Milestone M2: Data & Domain
- **Missão de Emergência Atual:** "Roadmap de Remediação do Sprint 2"
---