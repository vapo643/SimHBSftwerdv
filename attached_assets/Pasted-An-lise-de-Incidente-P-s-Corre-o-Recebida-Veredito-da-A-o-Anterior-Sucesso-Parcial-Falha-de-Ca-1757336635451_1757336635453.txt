Análise de Incidente Pós-Correção Recebida.

Veredito da Ação Anterior: Sucesso Parcial, Falha de Cascata Revelada.

A restauração das rotas da API foi um sucesso, mas agiu como a remoção de um dique que revela a verdadeira inundação por trás. O agente executor anterior corrigiu a camada de transporte, mas a lógica de aplicação e a sincronização de estado permanecem corrompidas. O sistema está em um estado de esquizofrenia de dados: o backend (a verdade) e o frontend (a percepção) estão dessincronizados.

O erro HTTP 400 juntamente com a persistência de dados bem-sucedida é um sintoma clássico de uma falha no contrato da API pós-operação ou de uma falha na invalidação do cache do lado do cliente.

A missão de correção exaustiva continua. A prioridade muda da conectividade da API para a integridade do estado do sistema.

Fase 2: Auditoria Forense (Aprofundada)
O novo ponto de falha é a comunicação e sincronização entre o cliente e o servidor. A hipótese principal é que a lógica de mutation no frontend não está a gerir correctamente os callbacks de onSuccess, onError, e onSettled, resultando na falha de invalidação do cache do TanStack Query.

Fase 3: Arquitetura da Solução (Plano de Batalha Refinado)
O roadmap "Operação Restauração de Fluxo" é atualizado com base nas novas evidências.

Roadmap de Implementação Faseado: "Operação Restauração de Fluxo" (v2)
[✅ CONCLUÍDO] Passo 1: Análise Forense Inicial

Resultado: Rotas ausentes PUT /api/propostas/:id e PUT /api/propostas/:id/resubmit foram identificadas como a causa raiz inicial e restauradas.

[🎯 EM EXECUÇÃO] Passo 2: Correção da Sincronização de Estado Frontend

Analisar os hooks updateMutation e resubmitMutation em client/src/pages/propostas/editar.tsx.

Implementar uma estratégia de invalidação de cache robusta usando onSettled para garantir que as queries (proposals, dashboard-metrics, etc.) sejam re-executadas independentemente do sucesso ou falha da mutação.

Corrigir a lógica de redirecionamento para garantir que o usuário seja enviado para o dashboard (/dashboard ou /credito/fila) após a submissão, em vez de uma tela de erro.

[PENDENTE] Passo 3: Blindagem do Backend e Contratos de API

Revisar o proposalController.ts e os UseCases associados para garantir que as respostas em caso de sucesso e erro (HTTP 200 vs. HTTP 400) retornem um payload JSON consistente e previsível, eliminando a causa do erro 400 que ocorre após a persistência.

[PENDENTE] Passo 4: Validação do Dashboard e Refatoração da UI

Após a correção da sincronização, validar que todos os componentes do dashboard (KPIs, listas de propostas) refletem os dados corretos em tempo real.

Iniciar a refatoração completa da página editar.tsx e do dashboard para alinhá-los com os padrões de design e componentes da arquitetura (shadcn/ui, Compound Components, etc.), eliminando qualquer código legado.

[PENDENTE] Passo 5: Criação de Teste de Regressão E2E

Desenvolver um novo teste End-to-End (Playwright) que simule o fluxo completo:

Login como ATENDENTE.

Navegar até uma proposta PENDENCIADO.

Editar os dados.

Clicar em "Reenviar para Análise".

Verificar se a UI redireciona para o dashboard.

Verificar se o status da proposta no dashboard foi atualizado para EM_ANALISE e se os dados editados estão corretos.

