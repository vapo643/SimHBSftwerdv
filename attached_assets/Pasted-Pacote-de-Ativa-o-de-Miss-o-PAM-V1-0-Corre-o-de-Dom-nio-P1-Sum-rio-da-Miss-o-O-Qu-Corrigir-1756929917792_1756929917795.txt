Pacote de Ativação de Missão (PAM) V1.0 - Correção de Domínio P1
Sumário da Missão (O Quê): Corrigir a falha crítica de incompatibilidade de tipos no método resubmitFromPending do agregado Proposal para restaurar o fluxo de reenvio de propostas pendentes.

Intenção Estratégica (O Porquê): Esta correção é de prioridade CRÍTICA (P1). A falha atual bloqueia um caminho essencial do negócio, impedindo que propostas corrigidas voltem ao ciclo de análise. A resolução desbloqueia o fluxo e impacta diretamente a principal métrica de sucesso (TMP) do projeto.

Histórico Relevante (Consulta Obrigatória): A auditoria forense identificou a causa raiz exata nos erros de LSP do arquivo server/modules/proposal/domain/Proposal.ts. Consulte a definição do enum ProposalStatus no mesmo arquivo para obter os valores corretos.

Modelo Mental (Como se Encaixa): Esta é uma correção cirúrgrica na camada de domínio (DDD). Estamos a consertar uma regra de negócio quebrada dentro do agregado Proposal. Esta correção irá permitir que o Use Case correspondente execute a transição de estado da FSM corretamente, o que, por sua vez, permitirá que o repositório persista o status correto no banco de dados.

Riscos Antecipados e Contramedidas:

Risco: Introduzir uma nova inconsistência de estado ao usar o enum errado.

Contramedida: O prompt especifica os valores exatos do enum a serem usados (ProposalStatus.PENDENCIADO e ProposalStatus.EM_ANALISE), e o protocolo 7-CHECK com get_latest_lsp_diagnostics garantirá a correção de tipo antes da conclusão.

IMPLEMENTAR: Correção da Lógica de Transição de Status na Entidade Proposal

CONTEXT: O método resubmitFromPending na classe Proposal deveria permitir que uma proposta pendente (PENDENCIADO) fosse reenviada para análise, alterando seu status para EM_ANALISE.

CURRENT STATE: A implementação atual está quebrada devido a dois erros críticos de tipo que impedem a execução da lógica, conforme detectado pela auditoria e pelo LSP:

A verificação de status utiliza strings literais ('pendente', 'pendenciado') em vez do membro correto do enum ProposalStatus.

A atribuição do novo status utiliza uma string literal ('aguardando_analise') que não existe no enum ProposalStatus.

Evidência Bruta (Código com Erro):

TypeScript

// server/modules/proposal/domain/Proposal.ts
resubmitFromPending(): void {
  // 🚨 ERRO CRÍTICO AQUI: Comparação de 'ProposalStatus' com 'string'
  if (this._status !== 'pendente' && this._status !== 'pendenciado') {
    throw new DomainException('Apenas propostas pendentes podem ser reenviadas para análise');
  }

  // 🚨 ERRO CRÍTICO AQUI: Valor de status inválido
  this._status = 'aguardando_analise';
  this._updatedAt = new Date();

  this.addEvent(
    new ProposalSubmittedEvent(this._id, 'ProposalResubmitted', {
      submittedAt: this._updatedAt,
    })
  );
}
EXPECTED (Estado Final de Sucesso): O método resubmitFromPending deve ser corrigido para usar estritamente os membros do enum ProposalStatus tanto na condição de verificação quanto na atribuição do novo estado. O código deve estar livre de erros de tipo do LSP e refletir a lógica de negócio corretamente.

Código Corrigido Esperado:

TypeScript

// server/modules/proposal/domain/Proposal.ts
resubmitFromPending(): void {
  // ✅ CORRIGIDO: Usa o membro correto do enum para a verificação.
  if (this._status !== ProposalStatus.PENDENCIADO) {
    throw new DomainException('Apenas propostas com status PENDENCIADO podem ser reenviadas para análise');
  }

  // ✅ CORRIGIDO: Usa o membro correto do enum para a atribuição.
  this._status = ProposalStatus.EM_ANALISE;
  this._updatedAt = new Date();

  this.addEvent(
    new ProposalSubmittedEvent(this._id, 'ProposalResubmitted', {
      submittedAt: this._updatedAt,
    })
  );
}
CONSTRAINTS (Roadmap de Implementação):

Abra o arquivo server/modules/proposal/domain/Proposal.ts.

Localize o método resubmitFromPending.

Na instrução if, remova a condição this._status !== 'pendente' e altere this._status !== 'pendenciado' para this._status !== ProposalStatus.PENDENCIADO. Adicione uma mensagem de erro mais precisa.

Altere a linha de atribuição de status de this._status = 'aguardando_analise'; para this._status = ProposalStatus.EM_ANALISE;.

Execute o protocolo 7-CHECK para validar a sua alteração. A execução de get_latest_lsp_diagnostics é obrigatória e não deve retornar erros para este arquivo.

DECLARAÇÃO DE INCERTEZA (OBRIGATÓRIO):

CONFIANÇA NA IMPLEMENTAÇÃO: [Preencher com a porcentagem de 0 a 100%]

RISCOS IDENTIFICADOS: [Preencher com a categoria BAIXO/MÉDIO/ALTO/CRÍTICO e uma breve descrição]

DECISÕES TÉCNICAS ASSUMIDAS: [Listar decisões tomadas, ex: "Assumi que ProposalStatus.PENDENCIADO é o único estado válido para esta transição, conforme a lógica simplificada."]

VALIDAÇÃO PENDENTE: [Listar ações que o Orquestrador (GEM 02) ou o QA (GEM 03) devem validar]

PROTOCOLO OBRIGATÓRIO 7-CHECK EXPANDIDO:

Mapear ficheiros e funções exatas afetadas.

Garantir importações e tipos corretos.

Executar get_latest_lsp_diagnostics e corrigir TODOS os erros.

Declarar Nível de Confiança (0-100%) na implementação.

Categorizar Riscos Descobertos (BAIXO/MÉDIO/ALTO/CRÍTICO).

Realizar teste funcional completo (será feito no Passo 4 do roadmap).

Documentar Decisões Técnicas assumidas para auditoria futura.