### Pacote de Ativação de Missão (PAM) NÍVEL DE CRISE - OPERAÇÃO CORRENTE DE CONFIANÇA V2

* **Sumário da Missão:** Erradicar a falha de configuração de ambiente que está a causar um "apagão" total em produção e desenvolvimento. A missão consiste em duas fases: uma **Fase de Diagnóstico Não Invasivo** para mapear o fluxo de configuração atual e uma **Fase de Correção Cirúrgica** para refatorar o código, eliminando a lógica de deteção de ambiente e estabelecendo uma única fonte de verdade para as configurações.
* **Intenção Estratégica:** Restaurar a operacionalidade do sistema em todos os ambientes e implementar uma arquitetura de configuração robusta que previna, de forma definitiva, a recorrência deste erro de "contaminação de ambiente". A confiança no sistema deve ser restabelecida através da transparência e da eliminação de "magia" no código.

---

**[DIRETRIZ DE PESQUISA WEB (MANDATÓRIA)]**

* Durante esta missão, você deve consultar a documentação oficial da **Supabase** e do **Vite.js** sobre as melhores práticas para o manuseamento de variáveis de ambiente (`environment variables`) em aplicações full-stack, especialmente a distinção entre variáveis de build-time (prefixo `VITE_`) e runtime.

---

### **FASE 1: DIAGNÓSTICO E MAPEAMENTO DA CONTAMINAÇÃO (AUDITORIA NÃO INVASIVA)**

**NÃO MODIFIQUE NENHUM CÓDIGO NESTA FASE. SUA MISSÃO É APENAS INSERIR LOGS.**

**1. Instrumentação do Backend:**
    * Abra `server/lib/config.ts`.
    * No início da função `getJwtSecret()`, adicione um log detalhado para expor as variáveis que ela está a ler:
        ```typescript
        console.log('[DIAGNOSTICO JWT] Tentando obter segredo. Valores atuais:', {
          NODE_ENV: process.env.NODE_ENV,
          DOMAIN: process.env.REPL_SLUG ? `${process.env.REPL_SLUG}.replit.dev` : 'localhost',
          DEV_JTW_SECRET_EXISTS: !!process.env.DEV_JTW_SECRET,
          PROD_JWT_SECRET_EXISTS: !!process.env.PROD_JWT_SECRET,
          SUPABASE_JWT_SECRET_EXISTS: !!process.env.SUPABASE_JWT_SECRET
        });
        ```

**2. Instrumentação do Frontend:**
    * Abra `client/src/lib/supabase.ts`.
    * Modifique a função `getSupabase` para expor as variáveis com que o cliente Supabase está a ser inicializado:
        ```typescript
        console.log('[DIAGNOSTICO SUPABASE FE] Inicializando cliente com:', {
          URL: import.meta.env.VITE_SUPABASE_URL,
          ANON_KEY: import.meta.env.VITE_SUPABASE_ANON_KEY
        });
        ```

**3. Relatório de Diagnóstico:**
    * Reinicie a aplicação de **desenvolvimento (Preview)**.
    * Tente fazer login.
    * Colete e apresente os logs do **console do servidor (backend)** e do **console do navegador (frontend)** que contêm o prefixo `[DIAGNOSTICO]`.
    * Execute o mesmo procedimento para o ambiente de **produção (Deploy)** e apresente os logs.

**EXPECTED (Entregável da Fase 1):** Um relatório claro mostrando o *mismatch*: os logs do frontend mostrarão as variáveis de produção, enquanto os logs do backend no ambiente de preview mostrarão que ele está a usar o `DEV_JTW_SECRET`.

---

### **FASE 2: CORREÇÃO CIRÚRGICA E BLINDAGEM DO AMBIENTE**

**APENAS APÓS A CONCLUSÃO E ANÁLISE DA FASE 1, EXECUTE ESTAS MODIFICAÇÕES.**

**1. Erradicação da Lógica de Deteção de Ambiente (Backend):**
    * Abra `server/lib/config.ts`.
    * **Refatore a função `getJwtSecret`**. Elimine completamente a função `detectEnvironmentFromDomain` e todas as condicionais `if (environmentType === ...)` de dentro dela. A nova função deve ler **apenas uma variável**, que será a `SUPABASE_JWT_SECRET`. Ela deve falhar ruidosamente se a variável não estiver definida.
        ```typescript
        // VERSÃO FINAL E SEGURA da getJwtSecret
        function getJwtSecret(): string {
          const secret = process.env.SUPABASE_JWT_SECRET;
          if (!secret) {
            console.error('[CONFIG] 🚨 FALHA CRÍTICA: A variável de ambiente SUPABASE_JWT_SECRET não está definida. O serviço de autenticação não pode iniciar.');
            throw new Error('Segredo JWT não configurado.');
          }
          console.log('[CONFIG] ✅ Segredo JWT carregado com sucesso.');
          return secret;
        }
        ```
    * Realize a mesma simplificação para as URLs de banco de dados e outras configurações. O código NUNCA deve tentar adivinhar o ambiente. Ele deve ler variáveis genéricas como `DATABASE_URL`, `SUPABASE_URL`, etc.

**2. Centralização da Configuração do Cliente Supabase (Frontend):**
    * Abra `client/src/lib/supabase.ts`.
    * Garanta que ele lê **apenas** `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`. Remova quaisquer outras variáveis de ambiente com prefixo `DEV_` ou `PROD_` do lado do cliente.

**3. Validação de Inicialização (Startup Check):**
    * No arquivo principal do servidor (`server/index.ts`), antes de iniciar o servidor Express (`app.listen`), adicione um bloco de verificação que chame a função `getJwtSecret()` e outras funções de configuração críticas dentro de um `try...catch`. Se qualquer uma delas falhar, o processo deve terminar com um erro claro (`process.exit(1)`), garantindo que o servidor nunca inicie num estado inconsistente.

**EXPECTED (Entregável Final):**
1.  O código-fonte modificado e limpo.
2.  Um relatório confirmando que a lógica de deteção de ambiente foi removida.
3.  Uma instrução clara para o Arquiteto Chefe (eu) sobre como limpar os `Secrets` do Replit, deixando apenas um conjunto de variáveis para cada ambiente (DEV e PROD), sem contaminação cruzada.