### Pacote de Ativação de Missão (PAM) V1.0

* **Sumário da Missão:** Realizar uma auditoria forense de layout na página "Fila de Análise" para diagnosticar a causa raiz de problemas de renderização (tela cortada e menu encolhido) específicos da role `ANALISTA`.

* **Intenção Estratégica (O Porquê):** A principal tela de trabalho para os analistas está visualmente corrompida, a impedir a eficiência operacional. É imperativo restaurar a funcionalidade visual completa para garantir uma experiência de utilizador consistente e profissional.

* **Histórico Relevante (Consulta Obrigatória):** O problema é descrito como uma tela que não ocupa todo o espaço disponível ("tela cortada") e um menu lateral que inicia "encolhido", impactando a usabilidade.

---

**IMPLEMENTAR:**
Auditoria Forense de Layout da Fila de Análise.

**CURRENT STATE:**
A página em `/credito/fila` renderiza de forma incorreta para a role `ANALISTA`, com o conteúdo principal não se expandindo para preencher a tela e o menu lateral começando num estado colapsado.

**EXPECTED (Estado Final de Sucesso):**
Um relatório de auditoria detalhado que identifique:
1.  O arquivo principal da página da "Fila de Análise" (provavelmente `client/src/pages/credito/fila.tsx`).
2.  A estrutura de layout principal que envolve esta página (provavelmente `client/src/components/DashboardLayout.tsx`).
3.  Uma análise do código CSS (classes Tailwind) e da estrutura JSX de ambos os arquivos.
4.  **Veredito Explícito:** Uma declaração clara sobre a causa raiz do problema. A falha está em classes de largura/altura no `fila.tsx`? É um problema com a definição do grid no `DashboardLayout.tsx`? Ou é uma falha na lógica do estado `sidebarOpen`? Aponte o(s) arquivo(s) e a(s) linha(s) de código exatas que estão a causar a anomalia.

**CONSTRAINTS (Roadmap de Auditoria):**
1.  Abra e analise o arquivo `client/src/pages/credito/fila.tsx`. Procure por quaisquer containers (`div`) com classes de largura fixa ou máxima (ex: `max-w-`, `w-screen`).
2.  Abra e analise o arquivo `client/src/components/DashboardLayout.tsx`.
3.  Inspecione a definição do layout principal em grid (ex: `lg:grid-cols-[280px_1fr]`) e verifique se alguma classe `overflow-hidden` ou similar pode estar a causar o corte.
4.  Analise a gestão de estado da `sidebarOpen`, incluindo o seu valor inicial e como ela é acionada, para entender por que o menu inicia encolhido.