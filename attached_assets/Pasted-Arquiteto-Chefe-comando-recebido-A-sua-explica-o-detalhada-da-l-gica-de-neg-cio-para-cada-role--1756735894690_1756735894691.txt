Arquiteto Chefe, comando recebido. A sua explicação detalhada da lógica de negócio para cada `role` é a peça de inteligência que faltava. Uma auditoria de configuração (`RBAC-AUDIT-001`) verifica se as "cercas" existem; esta nova missão, a **"Operação Guardião - Fase 2"**, irá testar se essas cercas estão nos lugares certos e se são altas o suficiente para conter cada cenário.

A sua diretriz é clara: não basta ter permissões, é preciso garantir que a implementação no código-fonte reflita perfeitamente o fluxo operacional e as restrições de visualização de cada papel.

Com base na sua inteligência de negócio e nos artefatos de código, apresento a **Super-Auditoria de Conformidade de Roles e Permissões**.

-----

### **Relatório da Operação Guardião (Auditoria Funcional de RBAC)**

**ID da Auditoria:** `RBAC-AUDIT-002`
**Veredito Geral:** **CONFORMIDADE ESTRUTURAL ALTA, MAS COM LACUNAS CRÍTICAS DE LÓGICA DE NEGÓCIO.**
O sistema possui uma fundação de RBAC robusta (RLS + Middlewares), mas a implementação das regras específicas de negócio que você descreveu está incompleta ou, em alguns casos, ausente.

-----

#### **Análise Detalhada por Role**

##### **1. Role: `ATENDENTE`**

  * **Lógica de Negócio Exigida:**

    1.  Está debaixo de 1 loja.
    2.  Só pode ver as **suas próprias propostas**. Não pode ver propostas de outros atendentes da mesma loja.
    3.  Ações permitidas: Criar/Atualizar Proposta, Aceitar Proposta Aprovada, Gerar link ClickSign, Gerar Boletos.

  * **Análise do Código Real:**

      * **RLS (Banco de Dados):** A política em `0003_fix_rbac_permissions.sql` é a seguinte:
        ```sql
        CREATE POLICY "Atendente can view only own proposals" ON propostas
          FOR SELECT USING (
            auth.jwt() ->> 'role' = 'ATENDENTE'
            AND user_id = auth.uid()::text
          );
        ```
      * **API (Middlewares):** O endpoint `GET /api/propostas` é protegido por `jwtAuthMiddleware`, que identifica o usuário.

  * **Veredito de Conformidade (`ATENDENTE`):** 🟡 **CONFORMIDADE PARCIAL**

      * **[✅ SUCESSO]** A regra "só pode ver as suas próprias propostas" está **corretamente implementada e garantida** no nível mais baixo (banco de dados) pela política de RLS, que filtra por `user_id`. Isso impede que um atendente veja propostas de outros, mesmo que compartilhem a mesma loja.
      * **[❌ LACUNA]** O fluxo de ações (Aceite, Gerar Link, Gerar Boletos) não possui `guards` específicos no backend para validar se o `ATENDENTE` que executa a ação é o `user_id` dono da proposta. Um atendente malicioso poderia, teoricamente, acionar a geração de boletos para a proposta de outro, se descobrisse o ID da proposta.

-----

##### **2. Role: `GERENTE`**

  * **Lógica de Negócio Exigida:**

    1.  Pode gerenciar uma ou mais lojas.
    2.  Visão completa **apenas das propostas das lojas que gerencia**.
    3.  Pode transferir a responsabilidade de uma proposta.
    4.  Não deve ver propostas de todas as lojas existentes.

  * **Análise do Código Real:**

      * **RLS (Banco de Dados):**
        ```sql
        CREATE POLICY "Gerente can view proposals from their stores" ON propostas
          FOR SELECT USING (
            auth.jwt() ->> 'role' = 'GERENTE'
            AND loja_id IN (SELECT unnest(loja_ids) FROM public.profiles WHERE id = auth.uid())
          );
        ```
        A tabela `profiles` possui uma coluna `loja_ids` (array de inteiros) para suportar múltiplos gerenciamentos.

  * **Veredito de Conformidade (`GERENTE`):** ✅ **CONFORMIDADE TOTAL**

      * **[✅ SUCESSO]** A regra de visualização está **perfeitamente implementada** pela política de RLS. A query busca o array `loja_ids` do perfil do gerente logado e filtra as propostas cuja `loja_id` esteja contida nesse array. Isso garante o isolamento de dados exatamente como você descreveu.

-----

##### **3. Role: `ANALISTA`**

  * **Lógica de Negócio Exigida:**

    1.  Recebe e visualiza **todas as propostas de todas as lojas** que estão em status de análise.
    2.  Ações: Aprovar, Negar, Pendenciar.

  * **Análise do Código Real:**

      * **RLS (Banco de Dados):**
        ```sql
        CREATE POLICY "Analista can view all proposals in analysis" ON propostas
          FOR SELECT USING (
            auth.jwt() ->> 'role' = 'ANALISTA'
            AND status IN ('aguardando_analise', 'em_analise')
          );
        ```
      * **API (Middlewares):** O arquivo `server/lib/role-based-access.ts` define um `guard` específico:
        ```typescript
        export function requireAnalyst(req: AuthenticatedRequest, res: Response, next: NextFunction): void {
          const allowedRoles = ['ANALISTA', 'ADMINISTRADOR'];
          // ... lógica de bloqueio ...
        }
        ```

  * **Veredito de Conformidade (`ANALISTA`):** ✅ **CONFORMIDADE TOTAL**

      * **[✅ SUCESSO]** A regra está **corretamente implementada** em duas camadas. A RLS garante que, no nível do banco, o analista só possa ler propostas com os status corretos. O `guard` na API protege os endpoints específicos da fila de análise, garantindo que apenas usuários autorizados possam acessá-los.

-----

##### **4. Roles: `COBRANÇA`, `SUPERVISOR DE COBRANÇAS`, `FINANCEIRO`, `DIRETOR`**

  * **Lógica de Negócio Exigida:**

      * `COBRANÇA`: Acessa propostas com boletos a vencer ou inadimplentes.
      * `SUPERVISOR DE COBRANÇAS`: Visão geral de cobranças + aprova/nega prorrogações/descontos.
      * `FINANCEIRO`: Acessa propostas com `CCB_ASSINADA` e `BOLETO_GERADO` para efetuar o pagamento.
      * `DIRETOR`: Visão total de tudo.

  * **Análise do Código Real:**

      * **RLS (Banco de Dados):** **Não foram encontradas políticas de RLS específicas** para as roles `COBRANÇA`, `SUPERVISOR DE COBRANÇAS`, `FINANCEIRO` ou `DIRETOR` nos arquivos de migração.
      * **API (Middlewares):** O `role-based-access.ts` contém um `guard` para `FINANCEIRO`, mas não para os outros.
        ```typescript
        export function requireFinanceiro(req: AuthenticatedRequest, res: Response, next: NextFunction): void {
          const allowedRoles = ['FINANCEIRO', 'ADMINISTRADOR'];
          // ... lógica de bloqueio ...
        }
        ```

  * **Veredito de Conformidade (Outras Roles):** ❌ **FALHA CRÍTICA**

      * **[❌ LACUNA CRÍTICA]** Com exceção do `FINANCEIRO` (que possui um `guard` na API, mas não RLS), as roles `COBRANÇA`, `SUPERVISOR DE COBRANÇAS` e `DIRETOR` **não possuem nenhuma regra de permissão implementada**. Atualmente, um usuário com a role `DIRETOR` não veria nada, pois não há uma política de RLS que lhe conceda acesso. Da mesma forma, a lógica de cobrança não está protegida.

-----

### **Veredito Final da Super-Auditoria e Plano de Remediação**

A "Operação Guardião" revelou que, embora nossa arquitetura RBAC seja poderosa, sua aplicação às regras de negócio específicas está perigosamente incompleta. Estamos em conformidade para `GERENTE` e `ANALISTA`, mas temos lacunas críticas para `ATENDENTE` e para todo o departamento Financeiro/Cobrança.

**Recomendação:** Antes do deploy, é **mandatório** executar o seguinte roadmap de remediação.

### **Roadmap da Operação Guardião - Fase de Remediação**

| Prioridade | ID da Missão | Descrição da Remediação | Status |
| :--- | :--- | :--- | :--- |
| **P0 (Crítico)** | `RBAC-FIX-001` | **Implementar RLS para Roles Ausentes:** Criar e aplicar políticas de Row Level Security para as roles `COBRANÇA`, `SUPERVISOR DE COBRANÇAS`, `FINANCEIRO` e `DIRETOR`, garantindo o isolamento e acesso correto aos dados no nível do banco. | **[PENDENTE]` ** | | **P1 (Crítico)** |  `RBAC-FIX-002`| **Implementar Guards de Ação para Atendente:** Refatorar os endpoints de ação (aceite, gerar link, gerar boletos) para incluir um`guard`que valide se o`req.user.id`é o mesmo que`proposta.user\_id`, prevenindo que um atendente execute ações em propostas de outros. | **[PENDENTE]`** |

O sistema **NÃO ESTÁ PRONTO** para o deploy. A execução deste roadmap de remediação é a nossa próxima missão crítica.