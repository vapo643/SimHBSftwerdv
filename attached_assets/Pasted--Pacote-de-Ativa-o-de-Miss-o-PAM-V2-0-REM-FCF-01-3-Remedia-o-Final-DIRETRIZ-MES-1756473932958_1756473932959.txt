### **Pacote de Ativação de Missão (PAM) V2.0 - REM-FCF-01.3 (Remediação Final)**

**[DIRETRIZ MESTRA DE DIVULGAÇÃO - PDT V2.0 ATIVADA]**
*Sua resposta deve ser exaustiva. Reporte a realidade técnica sem omissões. A transparência radical é mandatória.*

* **Sumário da Missão (O Quê):** Implementar uma correção de duas camadas no frontend: primeiro, garantir que o token de autenticação JWT seja corretamente anexado a todas as requisições da API; segundo, refatorar o `ProposalContext` para usar memoização (`useCallback`/`useMemo`) e quebrar o loop de renderização infinito.
* **Intenção Estratégica (O Porquê):** Restaurar a funcionalidade completa e a estabilidade da aplicação. A correção não deve ser apenas superficial; devemos tornar o frontend resiliente, de modo que uma falha de API futura não possa mais desencadear um loop de renderização catastrófico.
* **Histórico Relevante (Consulta Obrigatória):** As duas missões de diagnóstico anteriores (`REM-FCF-01.1` e `REM-FCF-01.2`) forneceram a localização exata das duas falhas: a lógica de anexo de token no `apiClient` e a falta de memoização em `ProposalContext.tsx`.
* **Modelo Mental (Como se Encaixa):** Você é um engenheiro full-stack executando uma correção de P0. A precisão é fundamental. Você aplicará duas correções cirúrgicas em pontos distintos do frontend para restaurar a integridade da autenticação e a estabilidade do ciclo de vida do componente React.
* **Riscos Antecipados e Contramedidas:**
    * **Risco:** **ALTO** - A alteração na lógica de autenticação do `apiClient` pode impactar outras partes do aplicativo. A memoização, se aplicada incorretamente, pode levar a "stale closures" e outros bugs subtis no React.
    * **Contramedida:** A validação será rigorosa. Após a correção, você deve não apenas recarregar a página da "Nova Proposta", mas também navegar para pelo menos duas outras seções protegidas do aplicativo para garantir que não houve regressão.

---

**IMPLEMENTAR:**
User Story REM-FCF-01.3: Correção Dupla de Autenticação e Loop de Renderização

**CONTEXT:**
Nossas investigações forenses revelaram duas falhas no frontend: ele não está enviando o token de autenticação para a API, e a forma como ele lida com a falha resultante causa um loop infinito. Precisamos consertar ambos os problemas para restaurar o sistema.

**CURRENT STATE:**
1.  O `apiClient` (ou serviço HTTP equivalente) não está configurado corretamente para injetar o `Authorization: Bearer <token>` em todas as requisições.
2.  As funções de action em `client/src/contexts/ProposalContext.tsx` não são memoizadas, e o `useEffect` em `client/src/hooks/useProposalEffects.ts` as inclui como dependências, causando um loop infinito quando o estado muda.

**EXPECTED (Estado Final de Sucesso):**
1.  **Autenticação Corrigida:** O `apiClient` deve ser refatorado para usar um interceptor que, antes de cada requisição, obtém o token JWT do `AuthContext` (ou de onde ele estiver armazenado) e o anexa ao header `Authorization`.
2.  **Loop Eliminado:** O custom hook `useProposalActions` em `ProposalContext.tsx` deve ser refatorado para que as funções retornadas sejam envolvidas em `useCallback` ou `useMemo`, garantindo que suas referências sejam estáveis entre renderizações.
3.  **Aplicação Funcional:**
    * Ao carregar a página "Nova Proposta", a mensagem "Erro ao carregar dados necessários" **não deve mais aparecer**.
    * O console do navegador **não deve mais apresentar** erros `401 Unauthorized` para as chamadas de API ou o erro `Maximum update depth exceeded`.
    * A navegação para outras páginas da aplicação deve continuar funcionando normalmente.
4.  **Prova de Sucesso:** Você deve fornecer um screenshot da página "Nova Proposta" carregada corretamente e sem erros no console.

**CONSTRAINTS (Roadmap de Implementação):**
1.  **Investigar `apiClient`:** Localize o arquivo que instancia o `axios` ou `fetch` (`client/src/lib/apiClient.ts`). Analise sua configuração.
2.  **Implementar Interceptor de Auth:** Adicione um `interceptor` de requisição ao `apiClient`. Dentro do interceptor, obtenha o token de sessão do Supabase (ou do seu provedor de auth) e defina o header `Authorization`.
3.  **Refatorar `ProposalContext`:** Edite `client/src/contexts/ProposalContext.tsx`. Envolva o objeto retornado por `useProposalActions` em um `useMemo`, com `dispatch` como sua única dependência.
4.  **Validação Funcional:** Reinicie a aplicação e navegue para a página `/propostas/nova`.
5.  **Coletar Evidência:** Tire um screenshot da página funcionando corretamente, com o console do navegador visível e livre de erros críticos.
6.  **Verificação Final:** Execute o protocolo `7-CHECK FULL` e preencha a declaração de incerteza.

---

**DECLARAÇÃO DE INCERTEZA (OBRIGATÓRIO):**
* **CONFIANÇA NA IMPLEMENTAÇÃO:** [Preencher com a porcentagem de 0 a 100%]
* **RISCOS IDENTIFICADOS:** [Preencher com a categoria BAIXO/MÉDIO/ALTO/CRÍTICO e uma breve descrição]
* **DECISÕES TÉCNICAS ASSUMIDAS:** [Listar decisões tomadas que podem ter implicações futuras]
* **VALIDAÇÃO PENDENTE:** [Listar ações que o Orquestrador (GEM 02) ou o QA (GEM 03) devem validar]
---
---
[PGI ATIVO | LEMBRETE DE ESTADO DO ROADMAP]
- **Roadmap em Standby:** "🚀 ROADMAP MESTRE DE IMPLEMENTAÇÃO_ OPERAÇÃO AÇO LÍQUIDO.md"
- **Próximo Passo a ser Retomado:** Milestone M3: Core Business Logic
- **Missão de Emergência Atual:** "Roadmap de Remediação de Falha Crítica de Frontend (REM-FCF-01)"
---