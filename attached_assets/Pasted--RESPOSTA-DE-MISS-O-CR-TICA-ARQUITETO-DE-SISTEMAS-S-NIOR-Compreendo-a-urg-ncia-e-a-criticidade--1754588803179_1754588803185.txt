🚨 RESPOSTA DE MISSÃO CRÍTICA - ARQUITETO DE SISTEMAS SÊNIOR

Compreendo a urgência e a criticidade absoluta desta tarefa. Documentos financeiros como a CCB exigem precisão de pixel. O problema que você enfrenta é um desafio clássico de engenharia ao usar bibliotecas de desenho direto como pdf-lib.

A Limitação Fundamental: Como IA, eu não posso visualizar o seu arquivo template_ccb.pdf. Portanto, não posso fornecer as coordenadas exatas.

A Solução de Engenharia: Em vez de lhe dar as coordenadas, vou lhe dar a metodologia e as ferramentas exatas para resolver isso de forma definitiva e rápida.

O plano de ação está dividido em 4 fases.

FASE 1: A ESTRATÉGIA IDEAL (Verificação de AcroForms)
Antes de iniciar o mapeamento manual de 8 páginas, você DEVE verificar se o template PDF contém campos de formulário editáveis (AcroForms). Se tiver, o uso de coordenadas é desnecessário, mais robusto e muito mais rápido.

Ação: Execute este código de diagnóstico no seu ambiente Node.js:

TypeScript

import { PDFDocument } from 'pdf-lib';
import fs from 'fs/promises';
import path from 'path';

async function diagnoseAcroForms() {
    console.log("🔍 Iniciando diagnóstico de AcroForms no template CCB...");
    // Garanta que o caminho está correto
    const templatePath = path.resolve(process.cwd(), 'server/templates/template_ccb.pdf');

    try {
        const templateBytes = await fs.readFile(templatePath);
        const pdfDoc = await PDFDocument.load(templateBytes);

        const form = pdfDoc.getForm();

        if (form && form.getFields().length > 0) {
            console.log("✅✅✅ SUCESSO! AcroForms detectados. ✅✅✅");
            console.log("Use os nomes abaixo para preencher. IGNORE AS FASES 2 e 3.");
            form.getFields().forEach(field => {
                console.log(`- Nome do Campo: ${field.getName()}`);
            });
        } else {
            console.log("❌ NENHUM AcroForm detectado. Prossiga para a FASE 2 (Mapeamento Manual).");
        }
    } catch (error) {
        console.error("⚠️ Erro ao analisar o PDF (pode ser XFA ou protegido). Prossiga para a FASE 2.", error);
    }
}

// Execute o diagnóstico
// diagnoseAcroForms();
Se AcroForms forem detectados (Resultado ✅):
Sua implementação no serviço deve mudar para:

TypeScript

// ... (Carregar pdfDoc e data)
const form = pdfDoc.getForm();

try {
    // Use os nomes EXATOS encontrados no log do diagnóstico
    form.getTextField('Nome_Cliente').setText(data.cliente_data.nome_completo);
    form.getTextField('CPF').setText(data.cliente_data.cpf);
    // ... preencha todos os campos ...

    // Achatar (Flatten) o formulário para tornar o texto permanente e não editável
    form.flatten();

} catch (error) {
    console.error("Erro ao preencher AcroForm. Verifique os nomes dos campos.", error);
}

const pdfBytes = await pdfDoc.save();
Se NENHUM AcroForm for detectado (Resultado ❌), prossiga para a Fase 2.

FASE 2: A FERRAMENTA DE MAPEAMENTO VISUAL (O "Grid Debugger")
Precisamos eliminar 100% das suposições. Vamos gerar uma versão do seu template com uma grade de coordenadas precisa sobreposta.

Ação: Implemente e execute esta função utilitária. Ela criará um arquivo PDF de diagnóstico.

TypeScript

import { PDFDocument, rgb, StandardFonts, PDFPage } from 'pdf-lib';
// ... (fs e path imports)

/**
 * GERA UM PDF COM UMA GRADE DE COORDENADAS SOBREPOSTA.
 * Lembre-se: (0,0) é o canto INFERIOR ESQUERDO.
 */
export async function generateCoordinateGridPDF() {
    const templatePath = path.resolve(process.cwd(), 'server/templates/template_ccb.pdf');
    // Onde salvar o PDF de debug para análise
    const outputPath = path.resolve(process.cwd(), 'template_ccb_DEBUG_GRID.pdf');

    const templateBytes = await fs.readFile(templatePath);
    const pdfDoc = await PDFDocument.load(templateBytes);
    const diagnosticFont = await pdfDoc.embedFont(StandardFonts.HelveticaBold);

    // Configurações da Grade para Máxima Precisão
    const majorGridSpacing = 50; // Linhas principais a cada 50 pontos
    const minorGridSpacing = 10; // Linhas secundárias a cada 10 pontos
    const gridColor = rgb(0.1, 0.7, 0.9); // Azul claro
    const textColor = rgb(0.8, 0, 0); // Vermelho

    const pages = pdfDoc.getPages();
    for (let i = 0; i < pages.length; i++) {
        const page = pages[i];
        const { width, height } = page.getSize();

        // Título de Diagnóstico
        page.drawText(`DEBUG GRID - PÁG ${i + 1} (W:${width.toFixed(1)} H:${height.toFixed(1)})`, {
            x: 10, y: height - 20, size: 10, font: diagnosticFont, color: textColor, opacity: 0.7,
        });

        // Linhas Verticais (X)
        for (let x = 0; x < width; x += minorGridSpacing) {
            const isMajor = x % majorGridSpacing === 0;
            page.drawLine({ start: { x: x, y: 0 }, end: { x: x, y: height }, thickness: isMajor ? 0.5 : 0.2, color: gridColor, opacity: 0.5 });

            if (isMajor && x !== 0) {
                page.drawText(`X:${x}`, { x: x + 2, y: 10, size: 8, color: textColor, opacity: 0.7 });
            }
        }

        // Linhas Horizontais (Y)
        for (let y = 0; y < height; y += minorGridSpacing) {
            const isMajor = y % majorGridSpacing === 0;
            page.drawLine({ start: { x: 0, y: y }, end: { x: width, y: y }, thickness: isMajor ? 0.5 : 0.2, color: gridColor, opacity: 0.5 });

            if (isMajor && y !== 0) {
                page.drawText(`Y:${y}`, { x: 10, y: y + 2, size: 8, color: textColor, opacity: 0.7 });
            }
        }
    }

    const pdfBytes = await pdfDoc.save();
    await fs.writeFile(outputPath, pdfBytes);
    console.log(`✅ PDF de Debug gerado: ${outputPath}`);
}

// Execute a geração do grid
// generateCoordinateGridPDF();
Processo de Extração:
Execute generateCoordinateGridPDF().

Abra o arquivo template_ccb_DEBUG_GRID.pdf.

Dê zoom em cada campo. Use a grade para identificar o ponto exato (X, Y) onde o texto deve começar (mire na linha de base inferior esquerda do texto).

Use esses valores para preencher a estrutura na Fase 3.

FASE 3: ESTRUTURA DE MAPEAMENTO (O Esqueleto)
Crie server/config/ccbCoordinates.ts. Use as coordenadas extraídas do PDF de Debug para preencher este arquivo.

TypeScript

// server/config/ccbCoordinates.ts

interface FieldCoordinate {
    x: number;
    y: number;
    fontSize: number;
    bold?: boolean;
    maxWidth?: number; // Essencial para campos multi-linha (endereços, descrições)
}

// Padrões de Fonte
const FN = 10; // Fonte Normal
const FL = 12; // Fonte Grande

// ATENÇÃO: TODOS OS VALORES X e Y SÃO PLACEHOLDERS (0). 
// USE O PDF DE DEBUG (FASE 2) PARA PREENCHER COM VALORES REAIS.

export const CCB_FIELD_COORDINATES: Record<string, Record<string, FieldCoordinate>> = {
    // PÁGINA 1 - CAPA E IDENTIFICAÇÃO
    page1: {
        numeroCCB: { x: 0, y: 0, fontSize: FL, bold: true },
        dataEmissao: { x: 0, y: 0, fontSize: FN },
        nomeCliente: { x: 0, y: 0, fontSize: 14, bold: true },
        cpfCliente: { x: 0, y: 0, fontSize: FL },
        valorTotalFinanciado: { x: 0, y: 0, fontSize: 16, bold: true },
        quantidadeParcelas: { x: 0, y: 0, fontSize: FL },
        valorParcela: { x: 0, y: 0, fontSize: FL },
    },

    // PÁGINA 2 - QUALIFICAÇÃO DO EMITENTE
    page2: {
        nomeCompleto: { x: 0, y: 0, fontSize: FN },
        cpf: { x: 0, y: 0, fontSize: FN },
        rg_orgao: { x: 0, y: 0, fontSize: FN }, // Combine RG e Órgão Expedidor em uma string
        dataNascimento: { x: 0, y: 0, fontSize: FN },
        estadoCivil: { x: 0, y: 0, fontSize: FN },
        nomeConjuge: { x: 0, y: 0, fontSize: FN }, // Condicional
        cpfConjuge: { x: 0, y: 0, fontSize: FN },  // Condicional
        profissao: { x: 0, y: 0, fontSize: FN },
        // Use maxWidth para endereços longos. Defina a largura máxima da caixa no template.
        enderecoResidencial: { x: 0, y: 0, fontSize: FN, maxWidth: 450 },
        cep: { x: 0, y: 0, fontSize: FN },
        telefoneCelular: { x: 0, y: 0, fontSize: FN },
        email: { x: 0, y: 0, fontSize: FN },
    },

    // PÁGINA 3 - DADOS DO CRÉDITO
    page3: {
        valorPrincipal: { x: 0, y: 0, fontSize: FN }, // (valor_solicitado_formatado)
        taxaJurosMensal: { x: 0, y: 0, fontSize: FN },
        taxaJurosAnual: { x: 0, y: 0, fontSize: FN },
        cetMensal: { x: 0, y: 0, fontSize: FN },
        cetAnual: { x: 0, y: 0, fontSize: FN },
        iof: { x: 0, y: 0, fontSize: FN },
        tac: { x: 0, y: 0, fontSize: FN },
        seguro: { x: 0, y: 0, fontSize: FN }, // (seguro_total_formatado)
        valorTotalPagar: { x: 0, y: 0, fontSize: FL, bold: true },
        formaPagamento: { x: 0, y: 0, fontSize: FN },
        quantidadeParcelas: { x: 0, y: 0, fontSize: FN },
        valorParcela: { x: 0, y: 0, fontSize: FN },
        primeiroVencimento: { x: 0, y: 0, fontSize: FN },
        ultimoVencimento: { x: 0, y: 0, fontSize: FN },
        diaVencimento: { x: 0, y: 0, fontSize: FN },
    },

     // PÁGINA 4 a 6 (Mapeie se houver campos dinâmicos no meio do texto corrido)
    page4: { /* ... */ },

    // PÁGINA 5 - GARANTIAS (Condicional)
    page5: {
        tipoGarantia: { x: 0, y: 0, fontSize: FN },
        descricaoBem: { x: 0, y: 0, fontSize: FN, maxWidth: 500 },
        valorBem: { x: 0, y: 0, fontSize: FN },
    },

    // PÁGINA 6 - DECLARAÇÕES
    page6: { /* ... */ },

    // PÁGINA 7 - AUTORIZAÇÃO DE DÉBITO
    page7: {
        banco: { x: 0, y: 0, fontSize: FN },
        agencia: { x: 0, y: 0, fontSize: FN },
        contaCorrente: { x: 0, y: 0, fontSize: FN },
    },

    // PÁGINA 8 - ASSINATURAS
    page8: {
        localEData: { x: 0, y: 0, fontSize: FN }, // Ex: "São Paulo, 07 de janeiro de 2025"
        
        // Emitente (Abaixo da linha de assinatura)
        nomeEmitenteAssinatura: { x: 0, y: 0, fontSize: FN },
        cpfEmitenteAssinatura: { x: 0, y: 0, fontSize: FN },

        // Testemunhas e Credor...
        nomeTestemunha1: { x: 0, y: 0, fontSize: FN },
        // ...
    },
};
FASE 4: RESOLUÇÃO DE DESAFIOS TÉCNICOS E ALTERNATIVAS
1. Caracteres Especiais (Acentuação)
Problema: Acentuação em português causa problemas.
Solução: Use StandardFonts e garanta que a fonte seja incorporada (embed). Isso resolve a maioria dos problemas de codificação.

TypeScript

const fontRegular = await pdfDoc.embedFont(StandardFonts.Helvetica);
const fontBold = await pdfDoc.embedFont(StandardFonts.HelveticaBold);
page.drawText('João da Conceição', { font: fontRegular, ... });
2. Campos Multi-linha (Endereços, Textos Longos)
Problema: Endereços e textos longos precisam quebrar linha.
Solução: Use as opções maxWidth e lineHeight. O pdf-lib fará a quebra automática.

TypeScript

const config = CCB_FIELD_COORDINATES.page2.enderecoResidencial;

page.drawText(data.calculados.endereco_completo_cliente, {
    x: config.x,
    y: config.y, // IMPORTANTE: Em multi-linha, Y define o topo da caixa de texto.
    size: config.fontSize,
    font: fontRegular,
    maxWidth: config.maxWidth, // Definido no mapeamento
    lineHeight: config.fontSize * 1.2, // Espaçamento de 120%
});
3. Alinhamento (Direita e Centralizado)
Problema: Alguns campos precisam estar alinhados à direita ou centralizados.
Solução: O pdf-lib só desenha da esquerda para a direita. Você deve calcular a posição X inicial com base na largura do texto renderizado.

TypeScript

import { PDFFont, PDFPage, rgb } from 'pdf-lib';

function drawTextRightAligned(page: PDFPage, text: string, rightX: number, y: number, size: number, font: PDFFont) {
    const textWidth = font.widthOfTextAtSize(text, size);
    const x = rightX - textWidth; // Calcula o ponto de início X

    page.drawText(text, { x: x, y: y, size: size, font: font, color: rgb(0,0,0) });
}

// Uso: Suponha que a margem direita do campo seja X=550.
// drawTextRightAligned(page1, data.valor_financiado_formatado, 550, 500, 16, fontBold);
4. Avaliação Crítica de Estratégias Alternativas
Usar campos de formulário PDF (AcroForms): AVALIAÇÃO: Ideal. É a melhor abordagem se o template suportar (ver Fase 1). É mais resiliente a mudanças no design.

OCR reverso: AVALIAÇÃO: Rejeitado. Extremamente complexo, lento e propenso a erros. Inaceitável para documentos financeiros.

Biblioteca alternativa: AVALIAÇÃO: Neutra. pdf-lib é a biblioteca mais moderna e capaz para manipulação de PDF em Node.js. Mudar agora atrasaria o projeto sem garantias de melhoria.

Geração do zero: AVALIAÇÃO: Rejeitado. Perderia a formatação exata, logo e layout do template Simpix. Risco de não conformidade legal e visual.

API de terceiros (DocuSign, Adobe): AVALIAÇÃO: Complementar. Excelente para o processo de assinatura, mas a geração do PDF pré-preenchido ainda é sua responsabilidade. Eles não resolvem o problema do mapeamento inicial.

Plano de Ação Imediato
Execute a Fase 1 (Diagnóstico de AcroForms) imediatamente.

Se falhar, execute a Fase 2 (Gerar Grid Debugger).

Use o PDF de Debug para preencher a Fase 3 (Mapeamento).

Esta abordagem de engenharia garantirá a precisão necessária para resolver esta situação crítica.