# MISSÃO DE IMPLEMENTAÇÃO (CONSERVADORISMO TÉCNICO): Refatoração Segura do API Client

**CONTEXTO ESTRATÉGICO:**
Este prompt é o resultado final de um "Debate Máximo". A sua missão é refatorar o nosso código para usar um `apiClient.ts` centralizado, seguindo uma estratégia **incremental e ultra-segura** para evitar as falhas catastróficas do passado.

---
**🚨 RESTRIÇÕES ABSOLUTAS DE SEGURANÇA (LEITURA OBRIGATÓRIA):**
1.  **NUNCA** execute operações `DELETE`, `UPDATE` ou `INSERT` no banco de dados para fins de teste.
2.  **NUNCA** crie, modifique ou exclua dados de produção durante a refatoração.
3.  O seu foco é **APENAS** refatorar o código, sem tocar nos dados existentes.
4.  Se precisar de testar, use `console.log()` ou dados mock, **NUNCA** dados reais.
5.  A violação destas regras representa uma falha crítica da missão.
---

**ROADMAP DE EXECUÇÃO (Sequencial e Verificado):**

**FASE 1: Preparação e Isolamento (Risco Zero)**
* **Passo 1.1:** Crie o ficheiro `/client/src/lib/apiClient.ts`. Por agora, implemente apenas um método `GET` básico para testar a conexão, como uma função `healthCheck()`.
* **Passo 1.2:** Valide que a aplicação ainda inicia e funciona corretamente com este novo ficheiro.

**FASE 2: Migração Gradual (Apenas Leitura - Read-Only)**
* **Passo 2.1:** Comece pela página de `Parceiros`. Modifique **apenas** a função que busca a lista de parceiros (`GET /api/parceiros`) para usar o novo `apiClient`.
* **Passo 2.2:** Valide que a página de Parceiros ainda carrega os dados corretamente.
* **Passo 2.3:** Repita este processo, **uma API de cada vez**, para as funções de listagem (`GET`) das outras páginas (Lojas, Usuários, Produtos, etc.), validando a cada passo.

**FASE 3: Operações de Escrita (Cuidadosa)**
* **Passo 3.1:** Após todos os `GET`s estarem a funcionar, migre as funções de `POST` (criação), uma de cada vez, validando após cada uma.
* **Passo 3.2:** Continue com as funções de `PUT` (atualização) e `DELETE` (exclusão), sempre uma de cada vez e validando.

**FASE 4: Limpeza e Finalização**
* **Passo 4.1:** Remova o código de busca de dados legado que foi substituído.
* **Passo 4.2:** Adicione um tratamento de erros robusto ao novo `apiClient`.

Execute este plano para entregar um `apiClient` centralizado e estável.