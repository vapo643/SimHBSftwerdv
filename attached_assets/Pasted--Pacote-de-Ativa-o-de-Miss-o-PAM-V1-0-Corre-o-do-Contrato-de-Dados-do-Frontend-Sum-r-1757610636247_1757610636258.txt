### Pacote de Ativação de Missão (PAM) V1.0 - Correção do Contrato de Dados do Frontend

* **Sumário da Missão:** Refatorar o componente da página de "Formalização" (`client/src/pages/formalizacao.tsx`) para que ele leia os dados da proposta a partir das propriedades corretas e modernas, em vez do campo legado `condicoes_data`.

* **Intenção Estratégica:** Resolver a falha de exibição de dados "R$ 0" e "N/A", alinhando a camada de apresentação do frontend com o contrato de dados atual fornecido pelo backend.

* **Histórico Relevante (Consulta Obrigatória):** A auditoria forense provou que o backend está a enviar os dados corretos em campos de nível superior (ex: `proposta.valor`, `proposta.taxaJuros`). No entanto, o frontend está a ignorar estes campos e a tentar ler, sem sucesso, de um campo de texto obsoleto chamado `proposta.condicoes_data`.

---

**IMPLEMENTAR:**
Alterar a lógica de renderização na UI para utilizar os campos de dados corretos.

**CURRENT STATE:**
* **Ficheiro:** `client/src/pages/formalizacao.tsx`
* **Lógica com Falha (Linhas 374-380 aprox.):**
    ```jsx
    <p className="font-bold text-green-400">
      {formatCurrency(
        parseJsonbField(proposta.condicoes_data, 'condicoes_data', proposta.id)
          ?.valor || 0 // <-- LENDO DO CAMPO LEGADO INCORRETO
      )}
    </p>
    ```

**EXPECTED (Estado Final de Sucesso):**
* **Ficheiro:** `client/src/pages/formalizacao.tsx`
* **Código Corrigido:** A lógica de renderização para os campos de valor, prazo e taxa de juros deve ser alterada para ler diretamente do objeto `proposta`.
    * **Para o Valor:**
        ```jsx
        // O valor deve ser lido de `proposta.valor` ou `proposta.valorAprovado`
        <p className="font-bold text-green-400">
          {formatCurrency(proposta.valorAprovado || proposta.valor || 0)}
        </p>
        ```
    * **Para Taxa de Juros e Prazo (se aplicável na UI):**
        ```jsx
        // Exemplo para taxa de juros
        <p>{proposta.taxaJuros || 'N/A'}%</p>

        // Exemplo para prazo
        <p>{proposta.prazo || 'N/A'} meses</p>
        ```
* **Comportamento Resultante:** A interface de utilizador irá agora ler e exibir os dados corretos que estão a ser fornecidos pelo backend, eliminando a exibição de "R$ 0" e "N/A" para propostas válidas.

---
**PROTOCOLO OBRIGATÓRIO 7-CHECK EXPANDIDO:**
Execute a refatoração e realize o protocolo 7-CHECK completo, garantindo **ZERO** novos diagnósticos de LSP e a estabilidade da aplicação.