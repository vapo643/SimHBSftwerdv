**IMPLEMENTAR:**
Reset completo da funcionalidade de download de boletos para o estado mais simples e limpo possível, removendo todas as tentativas anteriores de solução de bypass e sanitização.

**CONTEXT:**
Estamos a iniciar um "Debate Máximo" de 7 interações para resolver o bug do "vírus detectado". Nossas tentativas anteriores de bypass falharam e adicionaram complexidade desnecessária e instável ao código. A primeira etapa deste debate é uma **"purga"**: remover todas as soluções paliativas e retornar a uma implementação limpa e direta para que possamos diagnosticar o problema a partir de uma base de código confiável.

**CURRENT STATE:**
O código contém múltiplos serviços, rotas e lógicas de bypass (`mcafeeSpecificBypass.ts`, `/api/mcafee-bypass`, etc.) que foram adicionados para tentar contornar o problema do antivírus.

**EXPECTED (Estado Final de Sucesso):**
Um sistema onde a funcionalidade de download de boleto consiste em uma **única chamada de API direta ao `interBankService.obterPdfCobranca()`**, sem nenhuma camada de bypass, sanitização, conversão de formato ou dependência adicional. O código deve estar como era antes de qualquer tentativa de resolver o bug do vírus.

**CONSTRAINTS (Roadmap da Purga - Execute todas as etapas):**

1.  **DELETAR o ficheiro do serviço de bypass:** `server/services/mcafeeSpecificBypass.ts`.
2.  **DELETAR o ficheiro da rota de bypass:** `server/routes/mcafee-bypass.ts`.
3.  **DELETAR o ficheiro da página de teste:** `client/src/pages/mcafee-test.tsx`.
4.  **REMOVER o registro da rota de bypass** do ficheiro principal de rotas (`server/routes.ts`).
5.  **REMOVER a rota da página de teste** do ficheiro de rotas do frontend (`client/src/App.tsx` ou similar).
6.  **AUDITAR o `interBankService.ts`:** Garanta que a função `obterPdfCobranca()` não possui nenhuma lógica de sanitização ou modificação. A função deve apenas baixar e retornar o buffer binário diretamente do Banco Inter. Reverta quaisquer alterações que tenham sido feitas nela.

**Relatório Final:**
O critério de sucesso é um relatório confirmando que todos os ficheiros e lógicas de bypass foram removidos e que a funcionalidade de download está no seu estado mais puro e simples.