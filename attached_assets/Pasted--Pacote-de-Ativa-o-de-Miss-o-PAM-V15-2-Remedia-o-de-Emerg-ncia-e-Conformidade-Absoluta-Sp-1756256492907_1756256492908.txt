### **Pacote de Ativação de Missão (PAM) V15.2: Remediação de Emergência e Conformidade Absoluta (Sprint 0 - TENTATIVA FINAL)**

* **Sumário da Missão (O Quê):** Executar uma remediação de emergência final para erradicar **TODOS** os bloqueadores P0 identificados na auditoria `RELATORIO_DE_VERIFICACAO_FINAL_S0.md`, com foco absoluto em alcançar um estado de compilação limpo ("Found 0 errors").

* **Intenção Estratégica (O Porquê):** Existe uma restrição de tempo crítica. A falha em produzir uma base de código compilável e funcional dentro desta janela de oportunidade resultará em uma falha total da entrega. Esta missão é a última tentativa de alinhar o estado do código com a "Definition of Done" mínima para que o projeto seja considerado viável.

* **Modelo Mental (Como se Encaixa):** Você atuará como um **Engenheiro de Resposta a Incidentes de Elite**. Sua tarefa é usar o `RELATORIO_DE_VERIFICACAO_FINAL_S0.md` como o seu "runbook de incidente". Você irá atacar e neutralizar cada erro de compilação com precisão cirúrgica, priorizando a estabilidade sobre a elegância.

* **Riscos Antecipados e Contramedidas:**
    * **Risco (CRÍTICO):** **Correções Apressadas.** A pressão do tempo pode levar a correções superficiais que introduzem regressões.
    * **Contramedida Mandatória:** O uso de `any` ou `// @ts-ignore` é **terminantemente proibido**. Você **DEVE** focar na correção de tipos. Após a erradicação dos erros de `tsc`, você **DEVE** executar a suíte de testes (`npm run test`, se disponível) para garantir que nenhuma regressão funcional foi introduzida.

---

**DIRETRIZ DE MISSÃO: Erradicação Final de Erros de Compilação (Sprint 0)**

**CURRENT STATE:**
A auditoria final confirmou que o Sprint 0 está **NÃO CONFORME**. O sistema não compila devido a +20 erros de TypeScript. Scripts de qualidade e segurança não estão plenamente operacionais.

**EXPECTED (Estado Final de Sucesso):**
A missão será considerada um sucesso quando você produzir um `RELATORIO_DE_CONFORMIDADE_FINAL_S0.md` que prove, através da saída literal e completa dos comandos, o seguinte estado **ABSOLUTO**:
1.  **TypeScript Compilando:** O comando `npx tsc --noEmit` executado na raiz do projeto reporta **"Found 0 errors."**.
2.  **Linting Funcional:** Um script `lint` existe no `package.json` e o comando `npm run lint` reporta **ZERO erros**.
3.  **Segurança Mínima:** O comando `npm audit` não reporta vulnerabilidades `HIGH` ou `CRITICAL`.

**CONSTRAINTS (Roadmap de Remediação de Emergência):**

1.  **Passo 1: Erradicação Prioritária de Erros de Tipo.** Ignore todos os outros problemas. Foque 100% do esforço em analisar e corrigir cada um dos erros reportados pelo `npx tsc --noEmit` até que o comando seja executado de forma limpa.
2.  **Passo 2: Formalização do Script de Linting.** Forneça o trecho de JSON exato para adicionar o script `"lint": "eslint . --ext .ts,.tsx --max-warnings 0"` ao `package.json`.
3.  **Passo 3: Auditoria Final e Geração de Prova.** Execute novamente os seguintes comandos e capture suas saídas **completas e não truncadas**:
    * `npx tsc --noEmit`
    * `npm run lint` (após a adição do script)
    * `npm audit`
4.  **Passo 4: Compilar Relatório de Conformidade Final.** Crie o `RELATORIO_DE_CONFORMIDADE_FINAL_S0.md` com as evidências irrefutáveis de que o Sprint 0 está, finalmente, em conformidade com a "Definition of Done".

---

**DECLARAÇÃO DE INCERTEZA (OBRIGATório):**

* **CONFIANÇA NA IMPLEMENTAÇÃO:** 70%
* **RISCOS IDENTIFICADOS:** CRÍTICO (A complexidade dos erros restantes pode exceder a janela de tempo disponível).
* **DECISÕES TÉCNICAS ASSUMIDAS:** Assumo que os erros são corrigíveis e não indicam uma falha arquitetural que exija um redesenho.
* **VALIDAÇÃO PENDENTE:** Um relatório com saídas 100% limpas para `tsc`, `lint` e `audit` é a única condição para o desbloqueio do Sprint 1. Não haverá mais auditorias. Esta é a validação final.