**### Pacote de Ativação de Missão (PAM) V1.0**

* **Sumário da Missão (O Quê):** Realizar uma auditoria forense para capturar a "evidência bruta" do payload de dados que o backend (`GET /api/cobrancas`) envia para a "Tela de Cobranças".
* **Intenção Estratégica (O Porquê):** Estamos presos em um loop de erro crítico onde propostas aparecem "vazias" na Tela de Cobranças, embora os dados existam no banco. Antes de qualquer nova correção, precisamos da prova irrefutável para determinar se a falha está na query do backend ou na renderização do frontend.
* **Histórico Relevante (Consulta Obrigatória):** Falhamos em resolver isso antes. Sua missão agora não é interpretar ou corrigir. É **instrumentar e extrair**.
* **Modelo Mental (Como se Encaixa):** Você é um engenheiro de depuração. Sua tarefa é inserir um "grampo" na linha de comunicação entre o servidor e o cliente e nos mostrar exatamente o que está a ser transmitido.
* **Riscos Antecipados:** **Risco:** Uma análise incorreta nos manterá no loop de erro. **Contramedida:** A sua missão não é analisar. É **produzir o log exato** solicitado.

---

**IMPLEMENTAR:**
Auditoria de diagnóstico com instrumentação de log. **NÃO FAÇA CORREÇÕES.**

**CURRENT STATE:**
A "Tela de Cobranças" exibe uma proposta que deveria ter dados completos, mas os campos aparecem vazios.

**EXPECTED (Estado Final de Sucesso):**
O log completo e não editado do array de propostas que o endpoint `GET /api/cobrancas` envia como resposta.

**CONSTRAINTS (Roadmap de Diagnóstico Forense):**

**1. Instrumentação do Backend:**
    - **Ação:** Modifique o ficheiro `server/routes/cobrancas.ts`.
    - **Implementação:** Localize o endpoint `GET /api/cobrancas`. Vá até o final da lógica, **imediatamente antes** da linha `return res.json(dadosFormatados)` (ou similar).
    - **Adicione o Log Crítico:** Insira o seguinte log para imprimir a "evidência bruta" no console do Replit:
      ```typescript
      console.log('--- EVIDÊNCIA BRUTA DO BACKEND ---');
      console.log(JSON.stringify(dadosFormatados, null, 2));
      console.log('--- FIM DA EVIDÊNCIA ---');
      ```

**2. Execução do Teste:**
    - **Ação:** Com o log implementado, simplesmente acesse a "Tela de Cobranças" na interface do usuário. A ação de carregar a página irá acionar o endpoint e gerar o log que precisamos.

**Relatório Final:**
O critério de sucesso é a apresentação do **output completo do console do Replit**, contendo o JSON exato das propostas que o backend está a enviar para a "Tela de Cobranças". Esta evidência nos dará a direção para a próxima etapa do nosso debate.