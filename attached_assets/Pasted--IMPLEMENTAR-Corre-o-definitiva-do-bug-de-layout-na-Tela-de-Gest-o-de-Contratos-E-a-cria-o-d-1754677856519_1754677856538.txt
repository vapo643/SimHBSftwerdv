**IMPLEMENTAR:** Correção definitiva do bug de layout na "Tela de Gestão de Contratos" E a criação de um "Template de Rota Padrão" para prevenir futuras regressões de layout em todo o sistema.

**CONTEXT:** Estamos presos em um loop de erro crítico. Sempre que uma nova tela de alto nível é criada, como a `/gestao/contratos`, o menu de navegação lateral desaparece, quebrando a consistência da UI. Isso é uma falha arquitetural recorrente. A missão é corrigir o problema atual e, mais importante, implementar uma solução permanente para que este erro nunca mais aconteça.

**CURRENT STATE:** A rota `/gestao/contratos` renderiza seu conteúdo, mas o faz fora do layout principal da aplicação, resultando na perda da navegação compartilhada.

**EXPECTED (Estado Final de Sucesso):**
1.  A "Tela de Gestão de Contratos" é renderizada corretamente **DENTRO** do layout principal, com o menu lateral sempre visível e funcional.
2.  Um novo ficheiro-template reutilizável é criado para garantir que TODAS as futuras páginas que criarmos sigam a estrutura de layout correta por padrão.

**CONSTRAINTS (Roadmap de Correção Arquitetural):**

**1. Criação do Template de Rota Padrão (A Solução Permanente):**
    - **Ação:** Crie um novo ficheiro no diretório de rotas do nosso frontend (ex: `client/src/pages/`) chamado `_template.tsx`.
    - **Implementação:** Este ficheiro deve conter a estrutura mínima de uma página, já importando e utilizando nosso componente de Layout Principal (aquele que contém o menu lateral). O conteúdo principal do template deve ser um placeholder, como `<div>// O conteúdo da sua página vai aqui</div>`.

**2. Correção da Rota Atual (A Correção Imediata):**
    - **Ação:** Refatore o ficheiro da rota `/gestao/contratos`. Apague a estrutura de layout defeituosa atual e reconstrua-a usando o novo `_template.tsx` como base.
    - **Implementação:** Mova a lógica e os componentes da tabela de gestão de contratos para dentro da nova estrutura de layout correta.

**3. Atualização da Documentação de Processo (O Registro da Solução):**
    - **Ação:** Crie ou atualize um ficheiro na raiz do projeto chamado `PADROES_DE_CODIGO.md`.
    - **Implementação:** Adicione a seguinte regra de desenvolvimento a este ficheiro: "**NOVA REGRA MANDATÓRIA: Para criar qualquer nova página no sistema, o desenvolvedor deve obrigatoriamente copiar o ficheiro `_template.tsx` e renomeá-lo, em vez de criar um novo ficheiro do zero. Isto garante a consistência do layout.**"

**TESTE:**
- Valide que, ao navegar para a rota `/gestao/contratos`, a página carrega o conteúdo da tabela E o menu lateral permanece visível e funcional.
- Confirme que os ficheiros `_template.tsx` e `PADROES_DE_CODIGO.md` foram criados corretamente no projeto.