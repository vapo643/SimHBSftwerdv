### Pacote de Ativação de Missão (PAM) V1.0 - Sincronização de Mapeamento de Status

* **Sumário da Missão:** Refatorar a lógica de mapeamento de status no ficheiro `client/src/pages/formalizacao.tsx` para alinhar as traduções de texto com os novos status canónicos do sistema.

* **Intenção Estratégica:** Corrigir a exibição incorreta e incompleta de status na interface de "Formalização", garantindo que o utilizador veja uma representação textual correta e atualizada do estado real de cada proposta.

* **Histórico Relevante (Consulta Obrigatória):** A auditoria forense provou que a função `getStatusText()` e o dropdown "Gerenciar Status" contêm listas de status desatualizadas, o que causa a exibição de nomes obsoletos e a omissão de novos status.

---

**IMPLEMENTAR:**
Atualizar a função `getStatusText()` e as `options` do `select` de "Gerenciar Status" com os novos valores.

**CURRENT STATE:**
* **Ficheiro:** `client/src/pages/formalizacao.tsx`
* **Lógica com Falha #1 (`getStatusText`):**
    ```javascript
    const getStatusText = (status: string) => {
      const statusTexts = {
        // ... valores obsoletos e ausentes ...
        // FALTANDO: AGUARDANDO_ASSINATURA, ASSINATURA_PENDENTE
      };
      return statusTexts[status as keyof typeof statusTexts] || status;
    };
    ```
* **Lógica com Falha #2 (Dropdown):**
    ```html
    <option value="CCB_GERADA">Contratos Preparados</option>
    <option value="ASSINATURA_CONCLUIDA">Contratos Assinados</option>
    ```

**EXPECTED (Estado Final de Sucesso):**
* **Ficheiro:** `client/src/pages/formalizacao.tsx`
* **Código Corrigido #1 (`getStatusText`):** A função deve ser atualizada para incluir todos os novos status.
    ```javascript
    const getStatusText = (status: string) => {
      const statusTexts = {
        aprovado: 'Aprovado',
        documentos_enviados: 'Documentos Enviados',
        CCB_GERADA: 'CCB Gerada',
        AGUARDANDO_ASSINATURA: 'Aguardando Assinatura', // <-- ADICIONADO
        ASSINATURA_PENDENTE: 'Assinatura Pendente',   // <-- ADICIONADO
        ASSINATURA_CONCLUIDA: 'Assinatura Concluída',
        BOLETOS_EMITIDOS: 'Boletos Emitidos',
        PAGAMENTO_CONFIRMADO: 'Pagamento Confirmado',
      };
      return statusTexts[status as keyof typeof statusTexts] || status;
    };
    ```
* **Código Corrigido #2 (Dropdown):** As `options` do `select` devem ter as suas traduções corrigidas.
    ```html
    <option value="CCB_GERADA">CCB Gerada</option> <option value="AGUARDANDO_ASSINATURA">Aguardando Assinatura</option> <option value="ASSINATURA_PENDENTE">Assinatura Pendente</option> <option value="ASSINATURA_CONCLUIDA">Assinatura Concluída</option> <option value="BOLETOS_EMITIDOS">Boletos Emitidos</option> ```

---
**PROTOCOLO OBRIGATÓRIO 7-CHECK EXPANDIDO:**
Execute a refatoração e realize o protocolo 7-CHECK completo.