**BUG:**
O fluxo de dados do endereço do cliente está quebrado. Os campos de endereço (CEP, Rua, Bairro, etc.) estão a ser unidos incorretamente na "Nova Proposta" e depois renderizados como um bloco de texto bagunçado e no local errado na CCB em PDF, em vez de serem preenchidos nos seus respectivos campos.

**PROCESSO SISTEMÁTICO (Roadmap de Correção):**

**1. Auditoria e Correção na Origem (Tela "Nova Proposta"):**
    - **Ação:** Investigue o formulário de "Nova Proposta". Garanta que os campos de endereço (`CEP`, `Rua`, `Número`, `Complemento`, `Bairro`, `Cidade`, `Estado`) são componentes de input separados no frontend.
    - **Validação:** Confirme que, ao submeter o formulário, estes campos são enviados ao backend como chaves distintas dentro de um objeto JSON.
    - **Correção no Banco:** Audite a API que salva a proposta e garanta que este objeto de endereço estruturado seja salvo corretamente na coluna `cliente_data` da tabela `propostas`.

**2. Correção Cirúrgica no Gerador de PDF (`pdfGeneratorService.ts`):**
    - **Ação:** Esta é a tarefa principal. Dentro da função `generateCcbFromTemplate`, implemente a lógica correta de preenchimento.
    - **Implementação:**
        a. Remova completamente qualquer código antigo que esteja a imprimir o endereço de forma incorreta no topo do PDF.
        b. Acesse o objeto de endereço de dentro de `propostaData.cliente_data`.
        c. Utilizando o documento `MAPEAMENTO_COMPLETO_CCB_COORDENADAS.md` como sua **única fonte da verdade**, implemente chamadas `page.drawText()` **SEPARADAS E INDIVIDUAIS** para cada componente do endereço, posicionando-os em suas coordenadas exatas. Exemplo:
            - `Rua` + `Número` → Coordenadas do campo "Endereço".
            - `Bairro` → Coordenadas do campo "Bairro".
            - `CEP` → Coordenadas do campo "CEP".
            - `Cidade` → Coordenadas do campo "Cidade".
            - `Estado` (UF) → Coordenadas do campo "UF".

**3. Validação Final (Protocolo 5-CHECK):**
    - **Ação Obrigatória:** Após as correções, execute `get_latest_lsp_diagnostics` e corrija TODOS os erros de LSP antes de continuar.
    - **Teste Obrigatório:** Gere uma CCB para uma proposta de teste e valide VISUALMENTE que:
        - O endereço **NÃO APARECE MAIS** no topo da página.
        - CADA parte do endereço (Rua, Número, Bairro, Cidade, Estado, CEP) está perfeitamente alinhada em seu respectivo campo no template, conforme o mapa de coordenadas.

**DADOS REAIS:**
Utilize uma proposta existente que tenha um endereço completo e estruturado para validar a correção de ponta-a-ponta.