### Pacote de Ativação de Missão (PAM) V1.0 - Remediação das Lacunas do Sprint 0

* **Sumário da Missão (O Quê):** Corrigir as duas lacunas arquiteturais identificadas no Relatório de Auditoria Forense do Sprint 0 para alcançar 100% de conformidade com o roadmap.
* **Intenção Estratégica (O Porquê):** Solidificar a fundação do projeto, garantindo que a estrutura de diretórios do Monólito Modular esteja perfeitamente alinhada com a documentação arquitetural (DDD) e que o pipeline de CI/CD esteja completo, com a etapa de build de container ativada.
* **Modelo Mental (Como se Encaixa):** Você é um engenheiro de DevSecOps encarregado de realizar duas correções cirúrgicas: um refatoramento estrutural e uma reconfiguração do pipeline de CI. A precisão é fundamental.

---

**IMPLEMENTAR (Plano de Remediação):**
Execute a seguinte sequência de ações para remediar as lacunas identificadas.

**CURRENT STATE:**
O projeto possui uma estrutura de domínio em `server/contexts/` e um job de "Docker Build" desativado (`if: false`) no ficheiro `.github/workflows/ci.yml`.

**EXPECTED (Estado Final de Sucesso):**
1.  O diretório `server/contexts` é renomeado para `server/modules`.
2.  O job "Docker Build" no ficheiro `.github/workflows/ci.yml` está ativado.

**CONSTRAINTS (Roadmap de Implementação):**

**Passo 1: Alinhar Estrutura de Domínio (DDD)**
1.1. Renomeie o diretório `server/contexts` para `server/modules`.

**Passo 2: Ativar Build de Container no Pipeline de CI**
2.1. Abra o ficheiro `.github/workflows/ci.yml`.
2.2. Localize o job "Docker Build".
2.3. Altere a condição `if: false` para `if: true` para ativar permanentemente este job no pipeline.

**Passo 3: Validação Final**
3.1. Verifique se o diretório `server/modules` agora existe e se o diretório `server/contexts` foi removido.
3.2. Apresente o trecho do ficheiro `.github/workflows/ci.yml` para confirmar que a alteração na condição do job "Docker Build" foi salva corretamente.

---
**DECLARAÇÃO DE INCERTEZA (OBRIGATÓRIO):**
* **CONFIANÇA NA IMPLEMENTAÇÃO:** 99%
* **RISCOS IDENTIFICADOS:** BAIXO. A renomeação de diretórios pode exigir a atualização de caminhos de importação em outros ficheiros. O Agente deve estar preparado para identificar e corrigir quaisquer erros de importação resultantes.
* **DECISÕES TÉCNICAS ASSUMIDAS:** Assume-se que a renomeação de `contexts` para `modules` é a única ação necessária para cumprir o requisito de "Estrutura do Monólito Modular".
* **VALIDAÇÃO PENDENTE:** Confirmação de que os testes no pipeline de CI continuam a passar após as alterações.